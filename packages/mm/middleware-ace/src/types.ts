/**
 * Core domain types for the ACE (Adaptive Continuous Enhancement) middleware.
 *
 * Re-exports from @koi/ace-types (L0u) — the canonical source for ACE domain types.
 * This re-export preserves backward compatibility for existing consumers.
 */

export type {
  AceFeedback,
  AggregatedStats,
  BulletTag,
  CurationCandidate,
  CuratorInput,
  CuratorOperation,
  Playbook,
  PlaybookBullet,
  PlaybookSection,
  PlaybookSource,
  ReflectionResult,
  ReflectorInput,
  StructuredPlaybook,
  TrajectoryEntry,
} from "@koi/ace-types";
