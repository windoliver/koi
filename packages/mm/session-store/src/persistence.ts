/**
 * SessionPersistence — pluggable interface for durable session storage.
 *
 * The contract is defined in @koi/core (L0) and re-exported here
 * for backward compatibility.
 */

// Re-export L0 contract
export type { SessionPersistence } from "@koi/core";
