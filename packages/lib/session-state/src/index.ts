/**
 * @koi/session-state — Per-session state management with FIFO eviction for middleware authors.
 */

export { createSessionState } from "./session-state.js";
export type { SessionStateConfig, SessionStateManager } from "./types.js";
