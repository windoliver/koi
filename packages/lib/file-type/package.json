{
  "name": "@koi/file-type",
  "description": "Magic-byte MIME detection for user-originated file content — clipboard, @-reference, upload",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun ../../../scripts/run-tsup.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun run build && bun test"
  }
}
