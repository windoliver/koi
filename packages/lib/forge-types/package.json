{
  "name": "@koi/forge-types",
  "description": "Shared type + contract surfaces for the @koi/forge-* L2 packages (demand, candidate, artifact, policy, lifecycle, events). L0u — no logic beyond pure type guards.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test"
  },
  "dependencies": {
    "@koi/core": "workspace:*"
  }
}
