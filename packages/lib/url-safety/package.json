{
  "name": "@koi/url-safety",
  "description": "SSRF / private-IP / metadata-endpoint blocklist for outbound HTTP — shared safe-fetch utility",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun run build && bun test",
    "test:unit": "bun test src --exclude src/__tests__/api-surface.test.ts",
    "test:api": "bun run build && bun test src/__tests__/api-surface.test.ts"
  }
}
