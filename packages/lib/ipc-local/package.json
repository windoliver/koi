{
  "name": "@koi/ipc-local",
  "description": "Local in-memory IPC: MailboxComponent backed by in-memory storage with microtask dispatch, plus a MailboxRouter for in-process multi-agent communication",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test",
    "test:api": "bun test src/__tests__/api-surface.test.ts"
  },
  "dependencies": {
    "@koi/core": "workspace:*"
  }
}
