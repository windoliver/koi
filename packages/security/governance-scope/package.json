{
  "name": "@koi/governance-scope",
  "description": "Capability-attenuation wrappers — scoped filesystem (glob allowlist), scoped fetcher (URLPattern allowlist), scoped credentials (key allowlist)",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test"
  },
  "koi": {
    "optional": true
  },
  "dependencies": {
    "@koi/core": "workspace:*",
    "@koi/url-safety": "workspace:*"
  }
}
