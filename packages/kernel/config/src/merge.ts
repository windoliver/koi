/**
 * Re-export deepMerge from @koi/preset-resolver.
 *
 * Canonical implementation lives in @koi/preset-resolver (L0u).
 * This re-export preserves backwards compatibility for in-package consumers.
 */
export { deepMerge } from "@koi/preset-resolver";
