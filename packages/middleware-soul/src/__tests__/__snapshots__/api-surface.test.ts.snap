// Bun Snapshot v1, https://bun.sh/docs/test/snapshots

exports[`@koi/middleware-soul API surface . has stable type surface 1`] = `
"import { ContentInput } from '@koi/soul';
export { CreateSoulOptions, DEFAULT_SOUL_MAX_TOKENS, DEFAULT_USER_MAX_TOKENS, SoulMiddleware, createSoulMiddleware, enrichRequest, validateSoulConfig } from '@koi/soul';
import { ResolvedContent as ResolvedContent$1 } from '@koi/file-resolution';

/**
 * Soul/user content resolution — thin wrappers around @koi/file-resolution.
 *
 * Delegates to \`resolveContent()\` from \`@koi/file-resolution\` with the
 * appropriate \`allowDirectory\` flag for each use case.
 */

/** Resolved soul/user content ready for injection. */
type ResolvedContent = ResolvedContent$1;
/** Options for resolving a soul or user field from manifest config. */
interface ResolveOptions {
    readonly input: string;
    readonly maxTokens: number;
    readonly label: "soul" | "user";
    readonly basePath: string;
}
/**
 * Resolves soul content from a manifest config value.
 *
 * Supports three input modes:
 * - Inline: string containing newlines => used directly
 * - File: path to a single .md file
 * - Directory: path to a directory with SOUL.md (required), STYLE.md, INSTRUCTIONS.md (optional)
 */
declare function resolveSoulContent(options: ResolveOptions): Promise<ResolvedContent>;
/**
 * Resolves user content from a manifest config value.
 *
 * Same as soul resolution but:
 * - No directory mode (user is always a single file or inline)
 * - Missing file => warning + empty result (not an error)
 */
declare function resolveUserContent(options: ResolveOptions): Promise<ResolvedContent>;

/**
 * @koi/middleware-soul — Markdown-based agent character + user context injection (Layer 2)
 *
 * @deprecated Use \`@koi/soul\` instead. This package is a thin re-export shim
 * kept for one release cycle. It will be removed in the next breaking release.
 *
 * Reads markdown personality files (SOUL.md, STYLE.md, INSTRUCTIONS.md) and per-user
 * context files (USER.md), then injects them as stable system prompt prefixes via
 * wrapModelCall/wrapModelStream.
 */

/**
 * @deprecated Use \`ContentInput\` from \`@koi/soul\` instead.
 */
type SoulUserInput = ContentInput;

export { type ResolveOptions, type ResolvedContent, type SoulUserInput, resolveSoulContent, resolveUserContent };
"
`;
