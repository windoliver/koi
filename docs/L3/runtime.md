# @koi/runtime — Full-Stack Agent Runtime Integration

The canonical L3 integration layer. Wires every production-ready L2 package into a single coherent runtime surface, provides VCR cassette replay infrastructure for CI, and owns the golden query test suite that proves all L2 packages work end-to-end with a real LLM.

## Recent updates

Review-finding correctness sync: no runtime dependency-set change. The integrated
L2 packages received targeted hardening that flows through the runtime surface:
`@koi/tasks` local shell tasks now scrub inherited host env and terminate process
groups on cancel/timeout; `@koi/tools-bash` flushes trailing UTF-8 decoder state in
stream draining; `@koi/model-openai-compat` serializes image blocks for
vision-capable Chat Completions models and fails closed when vision is disabled;
`@koi/daemon` subprocess workers now use an allowlisted environment with explicit
request overrides; `@koi/sandbox-executor` accepts Bun's fd=3 protocol pipe as either
a file descriptor or `ReadableStream`; and `@koi/middleware-tool-selector` clears
stale evicted-turn tombstones when a fresh selector snapshot is recorded.

- 2026-05-10: `@koi/proactive` adds `CompositionCheckpointStore` contract + `createInMemoryCheckpointStore` (issue #1301 Part 2 foundation). New per-plan progress primitive complementing the existing per-side-effect `CompositionExecutionLog` — enables coarse-grained plan resume on restart. `save` validates `executionId`/`planHash` non-empty, integer `nextStepIndex >= 0`, and `stepResults.length === nextStepIndex`. Interface returns `T | Promise<T>` so future Temporal- and SQLite-backed implementations can satisfy the same shape. Executor wiring and durable backends are tracked as follow-up slices.

`@koi/auto-harness` wired (#1355): added as an opt-in dependency of `@koi/runtime`. `@koi/auto-harness` is an L3 meta-package (depends on `@koi/middleware-policy-cache` + `@koi/forge-policy` + `@koi/forge-verifier` + `@koi/harness-search` + `@koi/harness-synth`) that turns `ForgeDemandSignal`s from `@koi/forge-demand` into deployed bricks via a gated pipeline: `generate → verifyCandidate → forgeStore.save (draft) → evaluatePolicy → requestDeploymentApproval → deployCandidate → forgeStore.save (authoritative) → reconcile → register policyEntry`. Wiring at the runtime layer: `createRuntime({ autoHarness })` validates configuration before constructing the stack (caller-supplied `policy-cache` middleware is rejected; `policyVerifier` and `notifier` are required), splices the stack-owned `policy-cache` middleware AND a session-cleanup observer middleware (priority 950) into the runtime middleware chain, wraps the caller's `onDemand` so each demand signal is routed through `synthesizeHarness`, and threads per-stream `(sessionId, runId, agentId)` ownership into `forgeDemand.onSessionAttached` so the synthesis pipeline can reject any `policyEntry` whose `agentId` doesn't match the session that produced the signal. The runtime exposes `autoHarness: { middleware, cleanupMiddleware, synthesizeHarness, resetSession, maxSynthesesPerSession }` on the handle. Hardened across 20 rounds of adversarial review across two `/review-loop` runs: random-UUID draft-id namespace eliminates TOCTOU-overwrite of existing forge-store records; post-deploy bookkeeping failures (missing authoritative artifact, post-deploy save failure, reconciliation failure) return success+artifact instead of `null` so callers don't redeploy against an already-live state; `agent_repeated_failure` dedupe key includes `brickId` so distinct failing bricks for the same `agentType` don't share a bucket; per-stream session-end cleanup auto-releases ownership; `wrapOnDemandWithAutoHarness` resolves owner first (so caller's own `dismiss` can't blank the lookup) then fails closed when no owner matches; approval gate accepts only `kind: "allow"` (rejects `always-allow`). `stack.dispose()` releases the completed-triggers notifier subscription separately from `policyCacheHandle.dispose()`. See `docs/L2/auto-harness.md` for the full contract.

`@koi/daemon` remote worker backend (#1871, PR #2156): no API change at the runtime boundary, but `@koi/daemon` now exports `createRemoteBackend` — a Nexus-backed `WorkerBackend` that drives `workers.{spawn,events,status,terminate,kill,probe}` over a `NexusTransport`. Restart ownership stays reconciler-only; the new backend slots into the existing `SupervisorConfig.backends` registry alongside subprocess and tmux. Hardened across 10 rounds of adversarial review: cross-worker event scoping in poll batches, forward-compatible event parsing (skip unknown kinds, fail-closed on unknown `exited.state`), confirmed-exit semantics for terminate/respawn (status poll required before flipping cached liveness), `CONFLICT` on respawn when the prior generation is still alive, single-watcher enforcement to avoid shared-cursor races, lifecycle event dedupe across the spawn buffer and first poll. Heartbeat is opt-in via `supportsHeartbeat: true` so heartbeat-routed workers reach the backend only when the remote endpoint actually emits heartbeat events. See `docs/L2/daemon.md` for the contract.

`@koi/registry-nexus` + `@koi/search-nexus` wired (#1407, PR #2150): both packages added as `@koi/runtime` dependencies. `@koi/registry-nexus` is a Nexus-backed `AgentRegistry` adapter (dual-generation CAS, polling projection, deep-frozen entries, tombstoning for partial-failure recovery, reserved metadata keys). `@koi/search-nexus` is a Nexus-backed `SearchBackend` adapter (`Retriever + Indexer` over `search_retrieve`/`search_index`/`search_remove`, server-side `min_score` with defensive client-side post-filter that drops pagination metadata under version skew). Both packages are runtime-immutable adapters of L0 contracts — no tools, no middleware. Coverage: 4 standalone golden assertions in `golden-replay.test.ts` (no cassette needed; both packages are unit-testable via mock `NexusTransport`), 87 + 26 unit tests covering chaos paths (CAS conflict, terminate ambiguity, generation rollback, id reuse, deep-freeze, score boundary, batch boundary), plus opt-in integration tests gated by `KOI_E2E_NEXUS=1` against a live Nexus daemon. Hardened across 10 rounds of adversarial review: caller phase preserved on register so `created → running` transition events fire for `ChildHandle`/`spawn-child`; terminate ambiguous-failure tombstones rather than silently downgrading to `suspended`; `koi:status` trust check uses round-trip-through-Nexus-state instead of strict phase equality. See `docs/L2/registry-nexus.md` and `docs/L2/search-nexus.md`.

fs_semantic_search wired (#1319): no runtime *dependency-set* change — `@koi/fs-nexus`, `@koi/tools-builtin`, and `@koi/governance-scope` were already integrated. The new builtin tool `fs_semantic_search` is auto-exposed by `createBuiltinSearchProvider` whenever the active `FileSystemBackend` advertises a `semanticSearch` method. Shared-wiring gates the tool (and `Grep`, which also reads file contents) behind the existing `wantRead` boundary so a host that drops `read` from `filesystemOperations` cannot retain a back-door content-read path. Over-fetch math is **additive headroom** in both `@koi/fs-nexus` (`requestedLimit + min(requestedLimit*4, 800)` when `scope`/`minScore` is active) and `@koi/fs-scoped` (modest outer headroom that does not stack multiplicatively with the inner backend's own over-fetch); a `warning` field is emitted when the server hits the over-fetch cap and filtered results fall short of the requested limit. Three new golden assertions cover the tool descriptor, option forwarding/warning passthrough, and the recorded `fs-semantic-search.trajectory.json` ATIF cassette (21 steps, agent + tool + system).

`@koi/daemon` tmux worker backend (#1870): no API change at the runtime boundary, but `@koi/daemon` now exports a `TmuxBackend` alongside the existing subprocess backend. Sessions are persisted via `FileSessionRegistry` keyed by `WorkerId` so `BackgroundSessionRegistry.describeList()` and `watch()` see tmux-hosted workers identically to subprocess workers. Restart ownership remains reconciler-only (`restart: "temporary", maxRestarts: 0`); the daemon supervises tmux session lifecycle (create/list/kill) and surfaces fingerprint/PID drift through the same `Supervisor.health()` path. See `docs/L2/daemon.md` for the backend selection contract.

`@koi/federation` wired (#1372): added as a dependency of `@koi/runtime`. Phase 3 baseline — multi-zone agent coordination with a Nexus-backed `ZoneRegistry`, fixed-interval sequence-cursor sync engine (per-remote `lastSequence` cursor, health monitor that flips a remote to `offline` after `offlineAfterFailures` consecutive fetch errors, best-effort `federation.zone_disconnect` on dispose), and a `KoiMiddleware` that transparently routes cross-zone tool calls via `federation.zone_execute` when `ctx.metadata.targetZoneId` is set. Vector clocks, LWW conflict resolution, adaptive polling, snapshot truncation, and clock pruning are deferred to #1410 (Phase 4e). Two standalone golden queries cover the cross-zone routing path (`federation.zone_execute` invoked + `onDelegated` fired) and the local-zone passthrough; no cassette is required because the middleware contract is fully exercisable from in-memory `NexusTransport` fixtures. See `docs/L2/federation.md` for the full contract.

`@koi/daemon` TUI surface integration (#1944): no API change at the runtime boundary, but `@koi/daemon` now exports `FileSessionRegistry` as a public type so L3 wiring can hold a registry handle directly. Daemon-spawned subprocess workers can be observed end-to-end through the runtime's session-registry path: `BackgroundSessionRegistry.describeList()` + `watch()` populate `/bg` rows, `Supervisor.health()` + `watchAll()` populate the `/supervisor` view, and `attachAgentRegistry` mirrors lifecycle events into the parent reconciler's `AgentRegistry` so the manifest reconciler and the daemon supervisor agree on what is running. Restart ownership: reconciler-only — daemon-spawned children use `restart: "temporary", maxRestarts: 0` so the engine reconciler is the sole auto-restart authority and the daemon handles only on-path operator kills + teardown-failure quarantine.

`@koi/dashboard-api` wired (#1382, PR #2132): added as a dependency of `@koi/runtime`. The package is a headless HTTP/SSE handler exposing the v2 observability core — agents, sessions, metrics, traces, events, health — over 9 REST endpoints plus a `/events` SSE stream. Stateless and transport-pure: routing, bearer-token auth (constant-time compare, fail-closed when unconfigured), cursor pagination, and SSE framing live in the package; storage is injected via the `DashboardDataSource` contract which returns `Result<T, KoiError>` so backends classify errors instead of throwing. No tool surface and no middleware wiring — the package is exempt from cassette/golden-query coverage and is excluded in `scripts/check-golden-queries.ts`. See `docs/L2/dashboard-api.md` for the endpoint matrix and SSE protocol.

`@koi/forge-policy` wired (#1349, PR #2128): added as a dependency of `@koi/runtime`. Pure sync deterministic policy evaluator + same-instance audit log for forge candidates — no middleware, no tools, no LLM surface. Public API: `evaluatePolicy(candidate, config, options)` returns a closure-branded `PolicyEvaluation`; `createPolicyEvaluator(auditOptions)` returns an `{ evaluateAndRecord, log }` bundle that guarantees same-instance authenticity binding by construction (cross-bundle/worker mismatches fail closed with an actionable error pointing at `evaluateAndRecord`). Hardening across two `/review-loop` runs (20 rounds, 13 fixes): hostile-input boundary at `specJson` decoded via `JSON.parse` inside fail-closed `try/catch` with UTF-8 byte cap (`STRUCTURAL_BUDGET_BYTES = 10MB`); `complexityOf` scorer fails closed on NaN/Infinity/negative/non-number; scorer provenance `{id, version}` bound into `configFingerprint`; audit `sequence` reserved-then-committed so rejected fail-closed writes leave no gap on retry; `failClosedOnOverflowSinkError` retention strategy required at construction. Two standalone golden queries plus a 7-test integration suite (structuredClone fabrication rejection, independent factory sequences, scorer/key-order fingerprint stability). 149 unit + integration tests, ~98% line coverage. See `docs/L2/forge-policy.md` for the full contract.

`@koi/rlm-stack` wired (#1359, PR #2126): added as a dependency of `@koi/runtime`. The L3 meta-package composes `@koi/middleware-rlm` with thresholds coordinated against `@koi/context-manager` so RLM segments fire only when a single user input exceeds the entire context window (above the 75% hard-compact threshold). Default priority is `RLM_STACK_PRIORITY_FLOOR = 801` — strictly above `@koi/middleware-tool-disclosure` (800) so the engine sorter cannot place RLM ahead of tool-disclosure under equal-priority ambiguity. Two standalone golden queries cover the factory's threshold computation and `policyFor` classifier (passthrough/compact/virtualize). No cassette trajectory: the middleware fires only on oversized inputs that aren't representative of the goldens' fixture sizes; RLM behavior under load is covered by `@koi/middleware-rlm`'s own 100-test unit suite. See `docs/L2/rlm-stack.md` for the API and `Caveats` (resolveThresholds prefix-canonicalization limitations).

Review-finding wiring sync (#2121): no runtime dependency set changes. The
existing `@koi/tools-builtin` integration now has an explicit filesystem
`pathGuard` hook on read/write/edit factories and `fs_write` defaults
`createDirectories` to `false` when omitted, matching its schema instead of
inheriting backend auto-create behavior. The existing `@koi/tools-web`
integration now forwards `ToolExecuteOptions.signal` into fetch/search executor
calls so cancelled turns can abort slow providers promptly.

`@koi/middleware-user-model` wired (#1389): added as a dependency of `@koi/runtime`. The package fuses three observation channels — pre-action ambiguity detection, post-action correction (incl. optional drift detection sub-channel), and pluggable sensor enrichment via the L0 `SignalSource` contract — into a single `[User Context]` block injected at the head of `ModelRequest.messages`. Default priority `415` (`intercept` phase) places it outside permissions/audit but inside the model adapter. Each channel is independently optional and degrades gracefully: failing `SignalSource.read()` is skipped, memory recall throwing yields empty preferences, classifier failures fall through to fail-closed-on-drift / fail-open-on-salience per the doc. Two standalone golden queries cover the factory wiring (priority 415, all hooks present) and the `formatUserContext` rendering invariant ([User Context] open/close, preferences + sensor state lines). No cassette trajectory recorded — the middleware has no LLM-callable tool surface; the optional drift-detection LLM classifier is a caller-supplied function, mocked in unit tests via `createKeywordDriftDetector`. See `docs/L2/middleware-user-model.md` for the full contract.

`@koi/handoff` + `@koi/task-spawn` wiring (#1371, PR #2117): both L2 packages
are now `@koi/runtime` dependencies and ship golden-query coverage in
`src/__tests__/golden-queries.test.ts`. Coverage includes the happy-path
prepare→inject→accept pipeline plus integration corner cases: TTL-expired
envelopes hidden from middleware, double-`accept_handoff` rejected with
`ALREADY_ACCEPTED`, middleware reservation reverts on model throw, copilot
routing via live idle agent, `maxDurationMs` aborts hung spawn, unknown
`agent_type` returns enumerated error. See `docs/L2/handoff.md` and
`docs/L2/task-spawn.md` for the per-package state-machine and precedence
rules.

Review-finding wiring sync: no runtime dependency set changes. The existing
`@koi/scheduler` integration now persists a task's `running` status before
dispatch starts, so async stores cannot observe an executing task as still
`pending`; a failed status write prevents dispatch. The existing
`@koi/sandbox-os` integration no longer pre-creates missing `denyRead` paths on
the host before sandbox launch. Existing denied files/directories are still
masked, while absent paths are skipped so credential-like deny entries such as
`.netrc`, `.npmrc`, `.pypirc`, and `.git-credentials` are not materialized.

`@koi/session` cancel-resume surface (#2105, issue #1683): no L2 dependency
set changes — `@koi/runtime` already depends on `@koi/session`. Behavioral
additions surfaced through the existing dep:
`wrapAdapterWithStatePersistence` (caller-side wrapper that snapshots
`EngineState` on `done.stopReason==="interrupted"` and clears stale
checkpoints on every other terminal), `resumeWithEngineState` (extends
`resumeForSession` with `lastEngineState` + `lastPersistedAt`), and an
optional `expectedVersion?: number` parameter on the L0
`SessionPersistence.updateLastEngineState` contract for cross-process
CAS. Both bundled stores (in-memory + SQLite) honor `expectedVersion`
inside their transactions; stores without atomic CAS are refused at
wrap construction time. Failure modes: on `loadState` exception the
wrapper signals `onPersistError`, atomically clears the unloadable
checkpoint via CAS (CONFLICT → silent back-off so a concurrent newer
write isn't clobbered), and yields a synthetic `done(stopReason="error")`
without calling `inner.stream` — fail-closed so a richer adapter that
partially mutates its cursor before throwing can't leak that state.
Hardened across 10 review-loop rounds: cross-process CAS clobber, in-
memory poison shield not durable across processes, transcript-cursor
saveState/loadState made non-pass-through, exit-handler-after-OpenTUI-
destroyRenderer (status flip moved inside `shutdown()` so the L0
`status` lifecycle ("running" on start → "done" on clean exit) is
actually enforced), live-session-id rebind on picker resume. Two
persistent flip-flops (preserve-vs-clear loadState semantics; staged-
user reinjection) locked in per the skill protocol — see PR #2105 for
the full reasoning matrix. Golden replay coverage unchanged: this
package was already integrated; the change extends its surface, not
the integrated set.

`@koi/skill-distiller` wired (#2104, issue #1649): added as a dependency of `@koi/runtime` to provide auto-distillation of successful agent traces into reusable parameterized skill drafts. The package is library-only — runtime ships the building blocks (`createDistiller`, `createDistillationPolicy`, `createDedupeIndex`, `createAuditLog`, `createStagingQueue`, `createSkillStore`, `createRelevanceScorer`) but does not yet register an automatic post-task hook into the agent loop; production wiring (observe → policy → distill → stage → review → store) is intentionally deferred to a follow-up issue so callers control when distillation runs. Two standalone golden queries in `golden-replay.test.ts` (`Golden: @koi/skill-distiller`) cover end-to-end pipeline assembly and `DistillationPolicy.shouldDistill` behavior with a synthetic trace fixture. No cassette trajectory recorded — the package has no LLM-callable tool surface; the LLM is consumed *internally* by `Distiller` as the cheap-model summarizer and is mocked in tests via the `DistillerLLM` injection point. Hardened across 20+ adversarial-review rounds: mandatory `redactor` (or explicit `allowUnredactedTrace` opt-in), deep-clone-and-freeze on every store/staging/audit insert, hash-based dedupe in `SkillStore` (prevents content-equivalent drafts evicting unrelated skills under different names), contiguous-prefix tool grounding (no leading-step omission), single-use resource-literal coverage (paths/IDs/UUIDs/emails/JWTs/numeric-IDs-under-identifier-keys must be parameterized — even when the tool was invoked once), 1:1 variable-arg cover with whole-token matching (no `grid` satisfying `id`), nested-object leaf flattening (`copy.src` and `copy.dst` are independent), array-recursive resource scan, byte-accurate (UTF-8 via `TextEncoder`) prompt budget with fail-closed truncation, per-field caps in `parseSkillDraft` (16 KiB total + per-field length/count limits), and provenance hashes on the redacted trace. See `docs/L2/skill-distiller.md` for the full contract; 239 unit tests cover hash invariants, adversarial parser inputs, store LRU+hash interaction, staging state-machine edges, and a cassette-driven harness that replays every recorded ATIF trajectory through the full pipeline.

`@koi/middleware-policy-cache` wired (#2106, refs #1207): added as a dependency of `@koi/runtime`. The package short-circuits authorization decisions for forge-verified tool bricks promoted to policy mode — runs at `intercept@50` (outer of `permissions@100`), so a cached `block` synthesizes the canonical deny + dispatches a `PermissionDecision` to audit observers without ever entering permissions or the executor. **Trust model**: `register()` is fail-closed by default — every entry is rejected unless the host wires a `verifier` closure captured at construction time against forge's verified-set; `verifyOnHit` defaults to `true` so a mid-lifetime revocation re-binds trust on every hit (verifier-flip-false → tombstone + canonical deny). **Stale-event protection**: `StoreChangeEvent.generation` is honored end-to-end — `register()` refuses strictly older generations, the notifier path ignores stale events, and slot-replacement gates the strict downgrade corner (versioned cached entry displaced by unversioned incoming → refused with `VALIDATION` error). Forge-tools' in-memory store now emits `generation = storeVersion` on `saved`/`updated`/`removed` so the contract holds across the seam. Two standalone golden queries cover middleware shape (`policy-cache@intercept/50`, `wrapToolCall`, `describeCapabilities`) and composition correctness (cached `block` prevents inner permissions approval call). An 8-test cross-package integration suite under `packages/meta/runtime/src/__tests__/forge-policy-cache.integration.test.ts` exercises the real forge store ↔ real cache wiring: generation propagation through save/update/remove, matching-generation eviction, stale-event rollback protection, slot-replacement downgrade refusal, legacy-host displacement, and dispose unsubscribe. Hardened across three `/review-loop` runs (30+ rounds total, 13 fixes) covering attestation-token forging, brickId replay, post-registration mutation, generation-aware invalidation, per-turn block cap, dispatch timeout, full-bucket policy, agent-bucket LRU, executor closure trust, and verifier-throw-vs-revocation semantics. See `docs/L2/middleware-policy-cache.md` for the full trust model and config reference.

`@koi/mcp` registry-discovery surface (#1646): added `RegistryClient`, `RegistryCache`, `installMcpServer`, `uninstallMcpServer`, `pickPackageForInstall`, and `clearAllOAuthState` to the public API to support the new `koi mcp search/info/install/uninstall` CLI subcommands. Behavioral changes inside the existing surface: install verification runs verify-before-commit (no `.mcp.json` mutation until verify succeeds); OAuth tracking uses a per-server cleanup index with `withTrackedWrite` so cleanup and concurrent token/DCR-client writes serialize on the index lock (closes a credential-orphan race that could leave live OAuth material behind after uninstall/rollback); registry remote URLs are SSRF-checked (HTTPS-only outside loopback, RFC1918/link-local/CGNAT/multicast IP literals refused); `.mcp.json` reads reject malformed `mcpServers` shapes before any mutation. No new cassette/trajectory — registry discovery is plain HTTP without LLM tools — but the @koi/mcp lifecycle middleware that already participates in golden replays is unaffected.

Review fix sync: `@koi/middleware-intent-capsule` remains an optional runtime
dependency, but now has standalone golden coverage in `golden-replay.test.ts`.
The assertion exercises the public middleware contract without an LLM cassette:
session start signs the mandate, `wrapModelCall` verifies and injects the signed
mandate message when configured, and `onSessionEnd` removes the capsule so later
calls fail closed with `capsule_not_found`. The CLI wiring gate also now documents
the runtime-only/optional packages that are intentionally not default TUI imports:
external-agent discovery/monitor/procfs sidecars, HTTP gateway ingress,
programmatic-only RLM, ACE/intent-capsule opt-in middleware, and non-default
sandbox providers.

`@koi/playbook-store-sqlite` wired (#2088): added as a dependency of `@koi/runtime` to provide a durable, cross-process backend for ACE playbooks. The store uses WAL + foreign-keys + busy_timeout PRAGMAs, PRAGMA `user_version`-based migrations (currently v7 with a `store_identity` singleton table for resume-time file-replacement detection), and a single-writer file lock with atomic temp+link publish, kernel-bootId-aware crash recovery (Linux), in-process refcount for multi-handle reuse, and dead-PID reclamation. Surfaced through the L2 `PlaybookStore` contract from `@koi/ace-types`; the runtime itself does not auto-instantiate — the CLI's TUI activation path constructs the store when `ace.playbook_path` is set in the manifest. Two standalone golden queries cover migration to v7 and lock acquire/release semantics. No cassette trajectory recorded — pure persistence with no LLM-callable tool surface.

`@koi/governance-scope` wired (#1882 gov-15): added as a dependency of `@koi/runtime`. The package provides capability-attenuation wrappers — `createScopedFs` (glob allowlist + ro/rw mode), `createScopedFetcher` (URLPattern allowlist), and `createScopedCredentials` (key-glob allowlist with microtask timing-oracle defense) — that bound the blast radius of a compromised or prompt-injected tool. Runtime wiring threads the operator-supplied `network.allow` and `credentials.allow` manifest fields through to `createScopedFetcher` and `createScopedCredentials` before passing them to tool/provider factories. `@koi/skills-runtime` gains `createScopedSkillsRuntime` (closes the Skill-tool credential-scope bypass) and `createProgressiveSkillProvider` (session-snapshot stability for provider attach). One standalone golden query added to `golden-queries.test.ts` covering `createScopedCredentials` fail-closed behavior and `createScopedFetcher` URL-allowlist enforcement. No cassette trajectory recorded — the package is pure capability enforcement with no LLM-callable tool surface beyond `authed_fetch` (wired through the CLI preset stack). See `docs/L2/governance-scope.md` for the full security model.

`@koi/middleware-rlm` wired (#1358): added as a dependency of `@koi/runtime`. The package is **library-only / programmatic-activation only** — `createBuiltinManifestRegistry` deliberately does NOT register a manifest entry, so repo authors cannot accidentally enable RLM via `koi.yaml`; hosts that need oversized-request virtualization must register it via a custom `MiddlewareRegistry` and opt into `acknowledgeSegmentLocalContract: true` per known-safe turn. Two standalone golden queries cover middleware factory + segmentation; a 4-test cross-package integration suite under `packages/meta/runtime/src/__tests__/middleware-rlm.integration.test.ts` exercises the contracts the unit tests cannot reach: completed-segment text durability under thrown later segment (no data loss via `consumeModelStream`'s terminal `done`), zero-token usage heartbeats surfacing as custom `usage` EngineEvents (so `applyActivityTimeout` sees outward progress on text-only adapters), trusted `role:"system"` fail-closed behavior, and `KoiError`-shaped retry metadata propagation through the real terminal `done`. Hardened across 20 review-loop rounds (two `/review-loop` runs, 11 fixes total): heartbeat plumbing (rounds 1+4), double-emit usage de-dup (round 2), trusted role:'system' chunking guard (round 2), iterator/abort/init thrown-failure wrapping with KoiError preservation (rounds 6+7), `SegmentAbortError` exposing analyzer-visible fields directly (round 9), active-turn-only chunking (round 8 — earlier history is no longer rewritten), per-segment text emitted incrementally so completed-segment output survives later abort (round 8), all-message non-text guard (round 6), and bounded-wait iterator cleanup (prior-run round 9). The architectural N-fan-out concern (downstream observe/persistence middleware sees N segments per oversized turn) is documented in `docs/L2/middleware-rlm.md` and skipped as out-of-scope; the programmatic-only activation gate prevents accidental composition. See `docs/L3/rlm-stack.md` for the full stack contract.
Review lifecycle doc sync (#2081): no runtime dependency or wiring changes.
Integrated L2 package docs were refreshed for `@koi/task-tools`,
`@koi/tools-web`, and `@koi/middleware-memory-recall`: task-tools' source
comment now matches the existing seven-tool surface; tools-web unregisters
caller abort listeners after fetch/search settlement; memory-recall clears
per-session state on `onSessionEnd`.

`@koi/middleware-circuit-breaker` + `@koi/middleware-call-limits` + `@koi/middleware-call-dedup` wired (#1419): three new resilience-layer L2 packages added as dependencies of `@koi/runtime`. **Circuit breaker** (priority 175, intercept) wraps `wrapModelCall` + `wrapModelStream` with `createCircuitBreaker` from `@koi/errors` — keys per provider (default) or per-tenant via custom `extractKey(model, ctx)`; only provider-shaped HTTP statuses (top-level `status`/`statusCode` or `cause.code` ∈ `RATE_LIMIT`/`TIMEOUT`/`EXTERNAL`) count toward failures, so local middleware errors cannot poison the shared circuit; HALF_OPEN probe consumption is released on consumer cancellation; `maxKeys` is a hard cap (refuses insert when all entries OPEN/HALF_OPEN rather than evict an active circuit). **Call limits** (priority 175, intercept) enforces per-tool and global tool budgets plus a model-call budget shared across `wrapModelCall` and `wrapModelStream`; per-session counters reset on `onSessionEnd`. **Call dedup** (priority 150, intercept — runs before call-limits so cache hits do not burn quota) is opt-in: caching only happens for tools listed in `include`, and `DEFAULT_EXCLUDE` is a hard floor covering every shipped mutating/stateful-read tool (shell/fs writes including `fs_edit`, agent control plane, notebook mutations + reads, task board, code exec, watchers, forge spawn, ambient-state filesystem reads `fs_read`/`file_read`); cached responses are deep-cloned at the boundary so caller mutation cannot corrupt the store; concurrent identical misses coalesce; `request.signal`/`request.metadata` bypass cache + coalescing; `onSessionEnd` evicts the session's entries and bumps a per-session generation counter so late-completing tool calls cannot repopulate the cache for a terminated session id. Three standalone golden queries added per package to `golden-queries.test.ts`. Hardened across 9 review-loop rounds — see PR description for the full bug log.

Phase-2 bug-bash fixes (#2072): no L2 dependency set changes — only behavior fixes inside already-integrated packages. `@koi/middleware-permissions` no longer lets stored grants bypass explicit ask rules (closes the out-of-workspace `fs_read` gap). `@koi/middleware-audit` adds `createAuditMiddlewareComplianceRecorder` so governance compliance events flow through the same hash-chain + signing pipeline as model/tool calls. `@koi/audit-sink-ndjson` exposes a synchronous `logSync` path used by `audit.onSessionEnd` to guarantee the closing record reaches disk on shutdown. `@koi/governance-defaults` consumes the new compliance recorder factory. The runtime wiring in `packages/meta/runtime/src/runtime.ts` is unchanged; cassettes / golden queries unaffected.

`@koi/middleware-reflex` + `@koi/middleware-turn-ack` + `@koi/middleware-prompt-cache` wired (#1420): three optimization middlewares added as dependencies of `@koi/runtime`. All three are `koi.optional: true` — they ship with the runtime for golden-query coverage but are not auto-instantiated by the CLI/TUI (each requires explicit configuration). `@koi/middleware-reflex` (intercept phase, priority 50) short-circuits the model call when the latest user message matches a configured rule, returning a canned `ModelResponse` with `stopReason: "stop"`; cooldowns are session-scoped and cleared via `onSessionEnd`. `@koi/middleware-turn-ack` (resolve phase, priority 50) emits debounced `processing` and terminal `idle` channel statuses around each turn via `ctx.sendStatus`; all dispatches go through a `fireAndForget` helper that routes both synchronous throws and async rejections to the configured `onError` callback (default `console.warn`). `@koi/middleware-prompt-cache` (resolve phase, priority 150) reorders `system` messages to the prefix and attaches `CacheHints` to `request.metadata` under `CACHE_HINTS_KEY` (using metadata, not a WeakMap, so the hints survive downstream object-spread cloning); the static-prefix token estimator is inlined sync because `wrapModelStream` requires synchronous transformation. One cassette + ATIF trajectory (`prompt-cache`) covers the only middleware in the trio with observable model-round output (reflex short-circuits before the model fires; turn-ack is fire-and-forget status). Eight standalone golden queries cover reflex rule matching, turn-ack debounce semantics, and prompt-cache reorder/hint stability across `wrapModelCall` and `wrapModelStream`.

`@koi/forge-optimizer` wired (#1350): added as a dependency of `@koi/runtime`. Pure advisory L2 package — no tools, no middleware, no I/O — that operates over `BrickFitnessMetrics` and `BrickArtifact` to produce optimization signals callers can act on. Five exported helper families: `validateGraphTransition` / `validateStoreTransition` (lifecycle edges, with the latter additionally rejecting exits from terminal lifecycles to match `memory-store.update` semantics), `recordUsage` (pure, replay-safe, deterministic-not-exactly-once update over a fitness snapshot — heals corrupt prior counters/samplers and emits all detected issues through an optional `onIntegrity` callback so observability is preserved without freezing telemetry), `detectFitnessIntegrity` (out-of-band multi-issue integrity check; returns ALL corrupt fields so a repair pipeline can address everything in one pass), `computePerformanceScore` (P99-based scoring with recency decay; mirrors `@koi/validation/computePercentile` to stay consistent with the canonical fitness scorer; fail-closed against the full sampler-corruption invariant set), `suggestRetirement` / `suggestMerge` / `suggestSimplify` (advisory-only — never mutates the store; integrity findings carry `kind: "integrity"` and must not be auto-applied as `kind: "retire"`). Two standalone golden queries cover the public surface; an integration test in `runtime/__tests__/forge-optimizer-integration.test.ts` drives the optimizer against a real `createInMemoryForgeStore` and walks the full lifecycle transition matrix to catch contract drift between the optimizer's terminal-lifecycle set and the store's. The package is `koi.optional: true` — wired into runtime for golden-query coverage but not auto-attached to the CLI/TUI agent.

`@koi/forge-tools` wired (#2068): added as a dependency of `@koi/runtime`. The package provides four primordial tools — `forge_tool`, `forge_middleware`, `forge_list`, `forge_inspect` — that let the agent synthesize, persist, and discover content-addressed bricks (BrickArtifacts) at runtime. Storage is pluggable via the `ForgeStore` contract; the package ships `createInMemoryForgeStore` (process-scoped, ephemeral) which is what the runtime cassette and the CLI/TUI preset stack use. The CLI integration registers a `forge` preset stack in `DEFAULT_STACKS` so the four tools are auto-attached to the TUI agent (one shared store per process). Synthesis tools enforce sandbox policy (`tool` defaults to `DEFAULT_SANDBOXED_POLICY`, `middleware` to unsandboxed), lifecycle gating (only `active` globals are visible to non-owners), per-publisher identity partitioning in the BrickId, and canonical-encoded byte limits on every input field. A cassette + ATIF trajectory (`forge-synthesize`) drives the full middleware chain through `forge_tool` → `forge_list` → `forge_inspect`. Two standalone golden queries exercise tool creation and store lifecycle.

`@koi/permissions-nexus` + `@koi/audit-sink-nexus` + `@koi/nexus-client` wired (#1399): three new packages added as dependencies of `@koi/runtime`. `@koi/nexus-client` (L0u) is the shared HTTP JSON-RPC transport extracted from `@koi/fs-nexus` and consumed by all three nexus packages. `@koi/permissions-nexus` provides a local-first `PermissionBackend` with Nexus write-through and polling sync — the local in-memory store answers every check without a network hop while writes propagate to Nexus asynchronously. `@koi/audit-sink-nexus` is a batched `AuditSink` that writes audit entries to Nexus NFS under a configurable path prefix; NDJSON files rotate daily and writes are coalesced into 32-entry / 5-second batches to limit round-trips. Two standalone golden queries added to `golden-replay.test.ts` for each package, covering the primary backend lifecycle contracts. `@koi/fs-nexus` also updated: `decodeNexusContent` now recurses into nested `{ content: { __type__: "bytes" } }` envelopes returned with `return_metadata: true`, and `createNexusFileSystem` guards `config.transport` with `typeof === "object"` so manifest `transport: "http"` strings are ignored rather than treated as injected transports.

Nexus boot lifecycle + audit-poison parity (#1401 Phase 3): `@koi/nexus-client` gained `health()` on HTTP transports, a `kind` discriminator (`http | local-bridge | probe`), per-call `NexusCallOptions { deadlineMs, signal, nonInteractive }`, and `assertProductionTransport` (rejects missing `kind` and disposable `"probe"` transports). `@koi/permissions-nexus` exposes `bootSyncDeadlineMs`, `abortInFlightSync()` (backed by an internal `AbortController` so the in-flight read is truly cancelled), and `isCentralizedPolicyActive()`. `@koi/audit-sink-nexus` accepts an optional `onError` callback so write failures can fail-closed in parity with NDJSON/SQLite sinks. `@koi/fs-nexus` adds `createNexusProbeFactory` (sealed-capability factory for disposable local-bridge probes) and threads per-call options through both HTTP and local-bridge transports. The runtime layer (CLI) now exposes three boot modes: `telemetry` (default), `assert-transport-reachable-at-boot`, and `assert-remote-policy-loaded-at-boot` — see `docs/L3/cli.md` for the full wiring contract.

`@koi/long-running` wired (#1386): added as a dependency of `@koi/runtime` and
included in the `tsconfig.json` project references list (alongside `@koi/hash`,
required transitively by golden-replay tests). Two standalone golden queries
added to `golden-replay.test.ts` (`Golden: @koi/long-running — harness lifecycle`)
covering soft-checkpoint cadence and start/pause/resume phase transitions with
in-memory `HarnessSnapshotStore` + `SessionPersistence` stubs (no LLM, no
network). No cassette trajectory recorded — the harness wraps the engine loop
and is exercised standalone via the public `createLongRunningHarness` factory.

Doc-wiring sync for lib review fixes (#2063): refreshed integration notes for
`@koi/spawn-tools`, `@koi/tool-exec`, `@koi/tools-bash`, and `@koi/tools-web`.
Runtime wiring is unchanged. The integrated behavior now documents structured
`agent_spawn.context` forwarding, fail-closed `execute_code` tool-call budget
exhaustion, capped foreground Bash timeouts, and clamped web-search abort timers.

`@koi/governance-security` wired (#1397): added as a dependency of `@koi/runtime`. Two standalone golden queries added to `golden-replay.test.ts` (`Golden: @koi/governance-security`) covering `createRulesAnalyzer` SQL injection detection (critical) and `createPiiDetector` email address detection. No cassette trajectory recorded — the package is a pure analysis library with no LLM-callable tools.

`@koi/gateway-nexus` + `@koi/gateway-stack` wired (#1368, gateway-4): two new L2/L3 packages added as `@koi/runtime` dependencies. `@koi/gateway-nexus` is a Nexus-backed `SessionStore` — write-through local cache, coalesced async write queue (with bounded retry, drainPath ordering, tombstone-based delete-cancel-write protection), degradation state machine with `lastFailureAt` cooldown. After Nexus #4000 sunset removed bare `read`/`write`/`delete` JSON-RPC methods, the store calls `read_bulk`/`write_batch`/`delete_batch` and translates the in-band `"File not found"` shape back to `NOT_FOUND` so callers retain the same semantics. `@koi/gateway-stack` is the L3 wiring that composes core gateway + canvas + webhook + optional Nexus into one bundle, sets `preserveSessionsOnStop: true` automatically when a `nexusTransport` is provided, refuses ambiguous `deps.store + nexusTransport` pairings, and exposes a `health()` + `healthHandler` returning 200 healthy / 503 degraded. Both packages are exempt from cassette recording (`GOLDEN_QUERY_EXEMPT`) — gateway infrastructure that sits outside the agent loop. Coverage is the unit suites (54 + 12 tests) plus tmux-driven E2E harnesses against a real Nexus container under `packages/net/gateway-stack/scripts/` (soak, concurrent storm, mid-flight `kill -9` failover, REPL-driven multi-pane validation). Wiring the chat TUI as a remote `gateway-stack` client is tracked separately as #2122 (Phase 3-gateway-5).

`@koi/gateway-http` wired (#1639): added as a dependency of `@koi/runtime` together with the L0u `@koi/gateway-types` contract package. `@koi/gateway-http` is an L2 HTTP/WS ingress package — the production-mode counterpart to `@koi/gateway-webhook`, providing per-channel HMAC SHA-256 (over raw bytes, Slack-format `v0:{ts}:{body}`), per-tenant nonce + idempotency isolation, three-tier rate limiting (source-IP front-door, per-tenant bucket, `maxInFlight` cap), strict CORS allowlists, structured `gateway.request` / `gateway.startup` audit events, and an HTTP-first graceful shutdown that bounds `pauseIngress()` by the grace budget. The package is wired through the L0u `Gateway` contract (`@koi/gateway-types`) so it does not depend on `@koi/gateway` directly — peer L2 isolation is preserved. `@koi/runtime` declares it as a workspace dependency and adds the project reference, but does not re-export the surface; deployment commands (`koi up` / future `koi serve`) are expected to import `@koi/gateway-http` directly when wiring an HTTP-mode gateway, mirroring v1's `@koi/gateway-stack` pattern (#1368). The package is exempt from cassette recording (`GOLDEN_QUERY_EXEMPT`) because, like `@koi/gateway-webhook`, it is HTTP ingress infrastructure that sits outside the agent loop — no tool or middleware surface to exercise via LLM replay. Coverage is provided by the package's own 132-test suite (23 pentest scenarios, audit integration, shutdown drain e2e) plus 10 rounds of adversarial review hardening.

`@koi/gateway-webhook` wired (#1366): added as a dependency of `@koi/runtime`. The package is an L2 HTTP ingestion server — it converts inbound POST requests from external services (Slack, GitHub, Stripe, generic/Standard Webhooks) into `GatewayFrame` events. Provides HMAC-SHA256 signature verification over raw bytes (streaming `node:crypto` `createHmac`, no concat-buffer copy), four-phase idempotency protection (`tryBegin`/`renew`/`commit`/`abort`), and cooperative dispatcher cancellation via `AbortSignal`. The package is exempt from cassette recording (`GOLDEN_QUERY_EXEMPT`) because it is HTTP ingress infrastructure that sits outside the agent loop — it has no tool or middleware surface to exercise via LLM replay. Two standalone golden queries added to `golden-replay.test.ts` (`Golden: @koi/gateway-webhook`) covering factory config guards and idempotency store lifecycle.

`@koi/settings` wired (#1958): added as a dependency of `@koi/runtime`. The package is an L0u hierarchical settings cascade loader — it reads user (`~/.koi/settings.json`), project (`.koi/settings.json`), local (`.koi/settings.local.json`), flag (CLI `--settings`), and policy layers and merges them into a single `KoiSettings` value. `loadSettings()` is the main entry point; scalars use last-wins merge, permission arrays concat+dedup across layers, and the policy layer is fail-closed (throws on missing/malformed file). No `createRuntime()` surface change — `@koi/settings` is a passive dependency consumed by `@koi/permissions`'s new `mapSettingsToSourcedRules` bridge. Two standalone golden queries added to `golden-replay.test.ts` (`Golden: @koi/settings`) covering basic load and 5-layer cascade merge. No cassette trajectory recorded — the package performs no LLM interaction.

`@koi/tasks` local_agent lifecycle wired into CLI spawn stack (#1657): `@koi/tasks` was modified to add `createLocalAgentLifecycle`, which is now consumed by `@koi/cli`'s `spawn.ts` preset stack. The runtime integration is unchanged — `@koi/tasks` was already a dependency of `@koi/runtime` via the task-board tools wiring. The behavioral change is in `@koi/cli`: the spawn stack now auto-runs `local_agent` tasks from the task board using the agent's own `SpawnFn`, bridging the task board into the child-agent spawning system.

`@koi/middleware-permissions` bash AST spec-guard wired (#2029): `@koi/bash-ast` is now integrated into the permissions middleware via `resolveBashCommand` + `enableBashSpecGuard` (default on). Any bash tool call whose command cannot be proven safe by a static exact-argv allow rule is downgraded to `ask` before the standard permission evaluation, even when a broad glob rule like `bash:*` would have permitted it. See `docs/L2/middleware-permissions.md` and `docs/L2/bash-ast.md` for the full spec-guard contract. The `@koi/permissions` package also gains `IS_EXACT_MATCH` symbol stamping on non-glob allow rules, enabling the spec-guard to skip its canary probe when the backend already confirmed an exact match.
`@koi/file-type` wired (#1935): added as a dependency of `@koi/runtime`. Two standalone golden queries added to `golden-replay.test.ts` (`Golden: @koi/file-type`) covering PNG and PDF magic-byte detection. No cassette trajectory recorded — the package is a pure detection utility with no LLM interaction. `@koi/tool-browser` and `@koi/tui` also gained `@koi/file-type` as a direct dependency; their runtime wiring is unchanged.

`@koi/daemon` subprocess-isolated supervision (#1866): the daemon's `WorkerIpcMessage` L0 envelope and `validateWorkerIpcMessage` validator are now exported for hosts that subscribe to worker IPC traffic directly. The 24h terminal-record retention sweep (`sweepTerminalRecords`) is exposed on `SessionRegistry` so operators can prune stale subprocess records without restarting the supervisor. The `koi bg ps --all` flag now surfaces terminated records during the retention window, covered by the supervision end-to-end loop (in-process + subprocess tests in `packages/net/daemon/src/__tests__/`). Runtime surface is unchanged — these additions are consumed by `@koi/cli`'s supervision wiring bridge, not by `createRuntime()`.

Doc-wiring sync for integrated packages (#1966): refreshed L2 docs for `@koi/memory-fs`, `@koi/middleware-extraction`, and `@koi/middleware-memory-recall` to reflect the confidence trust model, fail-closed frontmatter parsing, confidence-inclusive exactReplay dedup, and display rounding fix introduced by this bug fix branch. No runtime wiring changes.

`@koi/middleware-ace` wired (#1715): added as a dependency of `@koi/runtime`. `createRuntime({ ace })` installs the ACE (Adaptive Continuous Enhancement) middleware at priority 800, phase `observe` — outer of feedback-loop so injection sees the final `systemPrompt` and trajectory recording captures the real outcome of every model + tool call. The middleware records per-session `TrajectoryEntry` items via `wrapModelCall` and `wrapToolCall`, consolidates them into versioned `Playbook` entries on `onSessionEnd` (`frequency × successRate × recency` scoring + EMA confidence blend), and prepends `[Active Playbooks]` to `request.systemPrompt` on subsequent sessions within a token budget. The package ships `createInMemoryPlaybookStore` and `createInMemoryTrajectoryStore` as default backends; persistent stores plug in via `AceConfig.playbookStore` / `AceConfig.trajectoryStore`. Skipped when a middleware named `ace` is already present in `config.middleware`. Future PRs add the LLM reflector + curator and the AGP promotion gate (provenance + version lineage types are already on `Playbook`). See `docs/L2/middleware-ace.md` for the staged rollout.

ACE promotion gate hardening (#1715 follow-up): the structured promotion path is now correctness-hardened against retry, restart, and concurrency edge cases that the initial wiring did not cover. Idempotent commits and rollbacks compare persisted proposal/evaluation payloads byte-for-byte (via stable JSON canonicalization) before acknowledging success — reusing an id with mutated operations or metrics is rejected instead of falsely returning `promoted`. Replay watermarks are now per-session (`StructuredPlaybook.reflectedStepIndexBySession`) so two sessions sharing one structured playbook cannot overwrite each other's positions; sqlite schema bumps to v8 with a new `reflected_step_index_by_session` JSON column (legacy scalar `last_reflected_step_index` retained as max-across-sessions). Tool calls record their immutable caller `turnIndex` instead of the mutable session counter, so a tool resolving after `onBeforeTurn(N+1)` is still attributed to turn N. Session teardown now rerun-loops the structured pipeline (≤3 iterations, bounded) to absorb late wrapper completions before sealing, and a per-state lifecycle nonce (`runId`) prevents stale wrappers from a prior `onSessionStart` leaking into a freshly reused `sessionId`. `NexusStructuredStoreConfig` now requires an explicit `requirePreProvisioned: boolean` — every deployment must consciously choose fail-open vs fail-closed for initial-create races. Nine new e2e tests (`packages/lib/middleware-ace/src/__tests__/e2e/`) exercise these scenarios across actual sqlite restarts; full middleware-ace + sqlite + nexus suite is 339 pass / 0 fail.

`@koi/middleware-intent-capsule` wired (#1883 gov-16): added as a dependency of `@koi/runtime`. The package implements OWASP ASI01 (Agentic Goal Hijacking) defense via cryptographic mandate binding — at `onSessionStart`, the agent's system prompt + objectives are canonicalized, SHA-256 hashed, and Ed25519 signed with a fresh per-session keypair (via `node:crypto generateKeyPairSync`); before every `wrapModelCall` and `wrapModelStream`, the mandate hash is re-derived from config and compared against the stored capsule. Any mismatch throws `KoiRuntimeError("PERMISSION")` with `reason: "capsule_violation"` before the model is invoked (fail-closed). SHA-256 is the only crypto on the hot path (`computeStringHash` from `@koi/hash`); asymmetric signature verification is deferred to custom verifiers for audit/forensic use cases. The `verifier` config field (injectable `CapsuleVerifier` from L0) enables deterministic testing of the violation path without mutating the system prompt. Two standalone golden queries in `golden-replay.test.ts` cover middleware shape and fail-closed behavior.

`@koi/middleware-feedback-loop` wired (#2004): added as a dependency of `@koi/runtime`. The package is now part of the default integration layer. It provides model response validation + retry, quality gates, transport-level retry, and forge tool health tracking (quarantine + trust demotion). This PR also hardens the implementation: `wrapModelStream` now buffers the full chunk stream and validates the `ModelResponse` before re-yielding chunks (same quality guarantees as `wrapModelCall`); `isQuarantined` always rechecks the store for immediate operator effect; BrickId-first keying in the ring buffer pools all tool aliases that map to the same brick into one sliding window; and the grace period logic correctly handles `lastPromotedAt === 0` (unknown history → skip grace, allow demotion). See `docs/L2/middleware-feedback-loop.md` for the full API and health state machine.

> **Partial-cause TIMEOUT no longer auto-retries (#1360):** `resolveRetryable()` previously inferred retryability from a partial `Error.cause` for both `RATE_LIMIT` and `TIMEOUT` when `retryable` was omitted. The transport-default set is now narrowed to `RATE_LIMIT` only — TIMEOUT delivery state is unknown after a watchdog deadline (the first attempt may have landed server-side and only the response path failed), so blindly replaying could duplicate non-idempotent model requests, tool calls, or billable operations. Producers throwing `new Error(msg, { cause: { code: "TIMEOUT" } })` MUST set `retryable: true` (or wrap in a full KoiError with phase metadata proving idempotency) to opt in. Full KoiError throws are unaffected — those carry their own `retryable` field and are routed through `isRetryable()` directly.

`@koi/forge-demand` wired (#1346): added as a dependency of `@koi/runtime`. `createRuntime({ forgeDemand })` installs the demand-detector middleware at priority 445 (outer of feedback-loop's 450 so the detector observes only committed state). Four triggers — `repeated_failure`, `capability_gap`, `latency_degradation` (`performance_degradation`), `user_correction` — emit `ForgeDemandSignal` for the auto-forge pipeline. The runtime auto-wires feedback-loop's `healthHandle` into forge-demand's `healthTracker` when `feedbackLoop.forgeHealth` is configured (live trackers exist); without it the latency trigger stays dormant and a startup warning fires (F64). `onSessionAttached` is required at the type level (F102) and validated at runtime (F96/F101): the scoped handle delivered to it is the only surface that can dismiss pending signals; `onDemand` alone is insufficient because a re-emitting condition would consume the session budget without an acknowledge path. Object-identity authorization (F127) on `onSessionEnd`, in-band VALIDATION classified as NEUTRAL (F108), and quarantine payload classified as failure (F104) are tested via 79 unit tests in `demand-detector.test.ts` plus the `Golden: @koi/forge-demand` block in `golden-replay.test.ts`.

`@koi/temporal` wired (#1356): added as a dependency of `@koi/runtime`. The package provides durable agent execution via Temporal — `createTemporalScheduler` (TaskScheduler over Temporal Workflows + Schedules) and `createTemporalSpawnLedger` (SpawnLedger backed by an in-process semaphore). Also exports `createTemporalWorker`, a worker factory with lifecycle-safe `dispose()` that handles the Temporal SDK's connection ref-count constraint (start→drain→close sequencing). Two standalone golden queries added to `golden-replay.test.ts` (`Golden: @koi/temporal`) covering the SpawnLedger acquire/release/capacity contract and the scheduler submit/cancel/query/stats contract via a mocked Temporal client. No cassette trajectory recorded — `@koi/temporal` is infrastructure (no LLM-callable tools). Integration tests gated on `TEMPORAL_INTEGRATION=true` exercise the full scheduler + worker lifecycle against a live Temporal server.

`@koi/debug` wired (#1374): `createDebugAttach` and `hasDebugSession` re-exported from `@koi/runtime/index.ts` alongside `createEventRingBuffer`, `matchesBreakpoint`, and the debug constants. The package is not auto-installed into `createRuntime()` — hosts opt in by injecting the middleware returned by `createDebugAttach()` via `dynamicMiddleware`. Two standalone golden queries added to `golden-replay.test.ts` (`Golden: @koi/debug`). No cassette trajectory recorded — the debug middleware is infrastructure (gate + event buffer) with no LLM interaction. `clearAllDebugSessions` and `createDebugObserve` are internal test helpers, not re-exported.

`@koi/browser-ext` + `@koi/browser-playwright` + `@koi/browser-a11y` wiring (#1609): `createBrowserBackend()` is now exported from `@koi/runtime`. It accepts a `BrowserBackendConfig` (either `{ kind: "playwright"; page: Page }` or `{ kind: "ext"; wsEndpoint: string }`) and returns a `BrowserDriver` that the host passes into `createRuntime({ browser: driver })`. The Playwright variant delegates to `@koi/browser-playwright`; the extension variant connects to a live Chrome tab via the MV3 extension + native-messaging host from `@koi/browser-ext`. Accessibility serialization is shared via `@koi/browser-a11y`. CLI tsup config adds `playwright` and `playwright-core` to the external list so their CJS internals are resolved from node_modules at runtime rather than bundled by esbuild.

---

Doc-wiring sync for this branch: integrated package notes for `@koi/agent-runtime`, `@koi/daemon`, `@koi/middleware-permissions`, `@koi/long-running`, and `@koi/task-spawn` were refreshed. For the autonomous restoration work, runtime wiring is unchanged: `@koi/runtime` still consumes those L2 packages through its existing integration points, while the new `@koi/autonomous` package remains an opt-in L3 composition facade above them.

`@koi/daemon` AbortSignal plumbing on `WorkerBackend.watch()` (#1865): L0 contract extended to `watch(id, signal?: AbortSignal)`. The supervisor now creates a per-worker `AbortController`, threads its signal into `backend.watch(...)` on spawn, and aborts it on `stop()` and `shutdown()` — backends that stall or drop their watch stream without emitting a terminal event no longer leak the supervisor's watch IIFE. Subprocess + fake test backends honor the signal via abort listeners that resolve parked awaits. Hardened across 10 review-loop rounds: every IIFE cleanup branch is generation-safe (`pool.get(id) === entry` identity check) with post-await TOCTOU re-checks, the tidy-cleanup liveness probe is bounded (250ms), `stop()` defers `activeIds` release until `entry.cleanupSettled` resolves to close the duplicate-spawn race window, the watch-stream-fault path only resolves `exitedPromise` on teardown success (preventing concurrent `stop()` from winning a false "exited" signal and clearing identity guards), the heartbeat-timeout failure path no longer force-resolves `exitedPromise`, and the subprocess prune timer is identity-checked plus preserved on abort-before-terminal-drain so dead state is eventually pruned. No runtime API surface change — internal supervisor plumbing only.

`@koi/artifacts` wiring (#1651, Plans 1-6): adds `artifacts: { store, sessionId }` to `RuntimeConfig` and exposes `runtime.artifacts: { store, provider }` on the handle. The provider attaches four tools (`artifact_save`, `artifact_get`, `artifact_list`, `artifact_delete`) bound to the supplied sessionId. The host owns the `ArtifactStore` lifecycle — `dispose()` does not close the store. Storage is split: metadata in SQLite, blobs in `@koi/blob-cas` (L0u, shared with `@koi/checkpoint`). Plan 3 (#1920) adds `ArtifactStoreConfig.policy?: LifecyclePolicy` (TTL + quota + per-name retention, freeze-at-save for TTL) plus operator-facing `sweepArtifacts()` / `scavengeOrphanBlobs()` on the store. Plan 4 (#1921) makes the open path local-only and moves blob-ready repair + Phase B tombstone drain onto a background worker (`workerIntervalMs`, default 30s) with an `onEvent` hook surfacing `repair_exhausted` / `transient_repair_error` for operator triage. Plan 5 (#1922) + Plan 6 (#1923) add a pluggable `ArtifactStoreConfig.blobStore` slot so hosts can swap the default filesystem CAS for `createS3BlobStore` (from `@koi/artifacts-s3`) when multi-host fleets need a shared blob layer — see [Backend selection](../L2/artifacts.md#backend-selection) for trade-offs (per-op network latency, delete HEAD+DELETE cost, explicit-credentials security model). The runtime surface is unchanged — hosts configure these fields when constructing the store. See `docs/L2/artifacts.md` for the full protocol + crash-recovery + lifecycle contract. `createArtifactToolProvider` is re-exported for hosts that want to wire artifacts without going through `createRuntime`.

`@koi/mcp` OAuth (#1296): adds RFC 7591 Dynamic Client Registration with RFC 7592 rollback on registration failure (HTTPS + same-origin + strict-child path SSRF guard on the management URI), RFC 8707 `resource` parameter on authorize + token (opt-out via `includeResourceParameter: false`), CAS-on-UUID `generation` invalidation for stale DCR records (legacy `(clientId, registeredAt)` fallback), tenant-scoped storage keys (4-tuple: `serverName`, `serverUrl`, `redirectUri`, `authority`), schema-validated persisted client info, refresh-path CAS on `(accessToken, refreshToken)` for non-rotating ASes, 408/425/429 classified as transient on both DCR and token endpoints (preserve tokens), and structured `OAuthFailureReason` surfacing through `onAuthFailure` for operator-actionable CLI messaging. Integration harness in `packages/net/mcp/src/oauth/__tests__/flow-integration.test.ts` exercises 8 end-to-end scenarios against a Bun.serve mock authorization server.

`@koi/mcp` now exposes an `AuthToolFactory` callback so auth-needed servers surface as `<server>__authenticate` pseudo-tools (CC pattern). The component provider reads `failure.error.code === "AUTH_REQUIRED"` and either invokes the factory (when supplied by the host) or falls back to skipped components. No new providers are added — existing wiring continues to work.

`@koi/mcp-server` gained a `__test-echo-server__.ts` dev helper (not exported) for E2E validation of #1852. No behavioral change to the package's public API or runtime integration.

`@koi/agent-summary` hardening (#1957): runtime-integrated summary flows now coalesce identical concurrent requests into one in-flight pipeline, emit `inflight.join` telemetry for joined callers, clone cache payloads defensively to prevent caller mutation poisoning shared entries, and classify bounded dependency stalls as retryable `EXTERNAL` model errors with timeout metadata. Live reliability coverage also tightens retry classification so non-retryable HTTP statuses fail fast instead of consuming retry budget.

---

## What This Enables

### One-Call Full-Stack Assembly

`createRuntime()` (via `@koi/engine`'s `createKoi`) assembles an agent with every production middleware, tool, channel, and backend already wired. Consumers don't manually compose packages:

```typescript
import { createRuntime } from "@koi/runtime";

const runtime = await createRuntime({ manifest, adapter });
for await (const event of runtime.run({ kind: "text", text: "Hello" })) { ... }
```

### Golden Query CI Coverage

Every L2 package wired into `@koi/runtime` must have:
- A `QueryConfig` entry in `scripts/record-cassettes.ts` exercising its primary tools/middleware
- A `fixtures/<name>.trajectory.json` recorded with a real LLM (ATIF v1.6)
- Assertions in `src/__tests__/golden-replay.test.ts` validating the trajectory

This ensures no L2 package is wired without proven end-to-end coverage.

---

## Integrated L2 Packages

| Package | Role | Golden query |
|---------|------|-------------|
| `@koi/debug` | Step-through debugger — `createDebugAttach` (single-attach, breakpoints, gate-based pause, ring buffer), `hasDebugSession`, `matchesBreakpoint`. Not auto-installed in `createRuntime()` — hosts opt in via `dynamicMiddleware`. Re-exported from `@koi/runtime` for downstream consumers | standalone |
| `@koi/agent-runtime` | Agent definition registry + built-in agent resolver | `spawn-agent` |
| `@koi/agent-summary` | Structured session summaries (#1645) — three-variant `SummaryOk` envelope (`clean` / `degraded` / `compacted`) with `meta.rangeOrigin` provenance, dependency-injected `modelCall`, three-layer untrusted-cache validation (Zod shape + identity + variant invariants), full `SkippedTranscriptEntry` fingerprint + `PROMPT_VERSION` in cache key. Range rejects compacted/parse_error transcripts; session exposes explicit `crashTailStrategy: "reject"/"drop_last_turn"/"include_all"` tradeoff. Hardening in #1957 adds single-flight coalescing per request hash, `inflight.join` wait telemetry, defensive cache cloning on read/write, and bounded timeout classification for dependency stalls. Consumed by `/summarize` TUI command. | `agent-summary` (ATIF v1.6 trajectory + `agent-summary.summary.json` sidecar from afterRecord) |
| `@koi/channel-cli` | CLI stdin/stdout channel adapter | standalone |
| `@koi/channel-fallback` | Wrapper that downgrades unsupported `ContentBlock` kinds to text before delegating to an inner `ChannelAdapter`. `wrapWithFallback(inner, opts)` returns a `Proxy` so prototype, readonly extension methods (`stampForCurrentCall`, `currentCallEpoch`), and full `sendUnsolicited(message, ...rest)` signatures forward intact. `OUTBOUND_EXTENSION_METHODS` apply image→`[image: alt](url)` / file→`[file: name](url)` text rewrites; non-extension methods pass through via `Reflect.get(...).bind(target)`. (closes #1364) | standalone |
| `@koi/channel-ide` | IDE-side `ChannelAdapter` over a line-delimited JSON-RPC transport (LSP-shaped). Outbound `ContentBlock[]` becomes one `notify` frame per `send()`; inbound JSON-RPC text frames map back to `InboundMessage`. Single-client gating via the transport — race-free for editor extensions that own the connection. (closes #1364) | standalone |
| `@koi/channel-mobile` | Mobile `ChannelAdapter` — `Bun.serve` WebSocket with HMAC-SHA256-tagged outbound metadata, single-client connection gate via `authenticate(headers)` handshake, `pushNotifier` fail-closed fallback when offline. Strict-by-default: bare `send()` without an HMAC tag or in-handler ALS context routes to push or rejects (no implicit live delivery). `replyToInbound(inbound, outbound)` HMAC-tags the outbound to the originating session via `metadata.__sessionTag`; cross-instance / forged tags are rejected by HMAC verification. `sendUnsolicited({recipient})` is the explicit host-initiated path with explicit recipient routing (no implicit fallback). Two-phase live delivery: client `ack` frame OR `ackTimeoutMs` timeout → push fallback (push carries `deliveryId` for end-to-end dedup). Inbound size cap enforced on UTF-8 bytes. `unsafeAllowEphemeralSigningSecret` + `unsafeAllowQueuedWriteAsDelivered` opt-ins required for single-instance test fixtures. Shutdown-during-unacked-send releases promptly via push routing; `disconnect()` rejects pending acks into push fallback. (closes #1364) | standalone |
| `@koi/channel-voice` | Realtime voice `ChannelAdapter` — pluggable `VoiceTransport` (audio frames in/out) + `Stt`/`Tts` providers. Per-call epoch tagging (`voiceCallEpoch`), per-session monotonic call-generation counter (`voiceSessionGen`), origin-thread fence (`voiceOriginThreadId`), and explicit host-driven `endCall(sessionId)` API form a hard call boundary that survives reconnect on a reused `sessionId`. `sessionsAwaitingNewTurn` is a hard deny after `endCall`: stamped sends carrying stale or fresh metadata are rejected until a new inbound clears the fence. `replyToVoiceInbound(inbound, outbound)` and session-aware `stampForCurrentCall(sessionId, outbound)` are the supported tagging paths; both force `threadId` to the originating session. Atomic-utterance contract: `transport.sendUtterance(sessionId, utteranceId, frames[], signal)` is invoked once with the full ordered audio sequence — the transport is responsible for `utteranceId`-keyed idempotent dedup. AbortController-driven TTS / send timeout poisoning persists across reconnect on the affected `threadId`. Watchdog auto-bumps session-gen if the host abandons a turn. Privileged extensions (`endCall`, `sendUnsolicited`) are gated by inherited-channel spawn-policy `mode === "all"`. (closes #1364) | standalone |
| `@koi/channel-email` | Email `ChannelAdapter` — IMAP poll/IDLE inbound + SMTP outbound (closes #1363). Two-phase idempotency (`tryBegin`/`commit`/`commitPoison`/`abort`) with reasons (`committed` / `in-flight` / `capacity-exhausted` / `poisoned`); poisoned `tryBegin` short-circuits to a 200-ack-equivalent (no redelivery). Durable `IngressQueue` with `awaitDrain` + `markDurable()` brand-symbol attestation enforced in `production: true` mode. Worker-first connect ordering: `startHandlerWorker` registers BEFORE `imap.onNewMessage` so a sync-delivered envelope at subscription time still drains. CAS-exhausted thread seed (8 conflicts) surfaces as `THREAD_SEED_CAS_EXHAUSTED` so the IMAP callback rejects and the message stays unread. Attachment-only / empty-body mail returns `PARSE_FAILED` (fail-closed). Outbound `format()` returns `FormatResult` with `UNSUPPORTED_BLOCK` for non-text content; pre-flight check before `reserveThread` and rolls back on internal format failure. Multi-recipient sends throw `MULTI_RECIPIENT_UNSUPPORTED` (no safe partial-acceptance resolution in v1). `awaiting-recovery` outbox state blocks subsequent sends with `THREAD_BLOCKED_PENDING_RECOVERY`. `dispatchInbound` throws `NO_HANDLER` when registration is racing rather than silent-dropping. | standalone |
| `@koi/channel-discord` | Discord `ChannelAdapter` — discord.js Gateway (`messageCreate`, `interactionCreate`) + REST send. Slash commands and button interactions (15-min token TTL) with optional per-command ephemeral selection and `slashCommandFallbackToChannel` after token expiry. Outbound packs into `MAX_ACTION_ROWS=5` / `MAX_ATTACHMENTS=10` / `MAX_EMBEDS=10` payloads, splitting cleanly at boundaries. Button `customId` encodes `<action>:<JSON.stringify(payload)>`; encoder rejects `:` in `action` to prevent inbound delimiter collision. `clientOwnedByAdapter` tracking: adapter-instantiated clients are `destroy()`'d on disconnect; injected clients only have their listeners detached so other consumers keep working. `TelegramPartialDeliveryError`-equivalent `DiscordPartialDeliveryError` flags retry-unsafe state after partial multi-step send. (closes #1362) | standalone |
| `@koi/channel-signal` | Signal `ChannelAdapter` — drives `signal-cli --jsonRpc` over a `SpawnFn`-injected subprocess (Bun-only default; Node consumers must inject a `child_process.spawn`-based `SpawnFn`). Per-RPC request/response correlation with `SIGNAL_RPC_TIMEOUT_MS=30000` timeout. `SignalAmbiguousDeliveryError` for post-stdin-write delivery uncertainty (do NOT auto-retry); `SignalPartialDeliveryError` for multi-chunk failures. Inbound DM normalization drops messages with no replyable sender (non-E.164, no group). Unexpected subprocess exit forces adapter into disconnected state, fires `onUnexpectedExit` once, clears `pendingEvents`, and rejects subsequent `send()`. `onHandlerError` callback surfaces inbound dispatch failures that would otherwise be swallowed by `Promise.allSettled`. E.164 enforcement at construction. (closes #1362) | standalone |
| `@koi/channel-slack` | Slack `ChannelAdapter` — Socket Mode (WebSocket via `@slack/socket-mode`) and HTTP Events API (HMAC signature + replay window). Form-payload classification runs before the handler-readiness gate so unsupported interactive callbacks (view_submission, shortcut) get a 200 within Slack's 3s modal contract even during startup churn; unsupported `event_callback` subtypes return 400 (visible in Slack's app dashboard) and unsupported interactive types fire `onUnsupportedEvent({kind, type})` for operator visibility. Hook supports sync + async; `fireUnsupportedEvent` contains both throws and rejections so observability code cannot break the ack timing. Bot identity provenance: events with no `user` resolve to `bot:<bot_id>` (events with neither are dropped). Channel-less `block_actions` (modal/home-tab) drop rather than synthesize a `threadId: "unknown"` reply target. `replyToMode: "all" \| "off"` (`"first"` rejected at construction). Idempotency-Key dedupe scoped to `event_id` for events and signed-body hash for slash/interactive. | standalone |
| `@koi/channel-teams` | Microsoft Teams `ChannelAdapter` — Bot Framework webhook inbound + Graph/Bot Connector outbound (closes #1363). JWT verifier with `createRemoteJWKSet` hoisted outside the per-token closure so JWKS cache is shared across requests. Cloud profiles: `public` / `gov` / `inline` — inline cloud requires explicit `tenant`/`scope` at construction, otherwise throws `INVALID_CONFIG`. Inline-cloud minter is deferred (returns a stub that throws on `appToken()`) so inbound-only deployments can construct the verifier without outbound credentials. Conversation address store seeds a `ConversationAddress` from lifecycle activities (`conversationUpdate`, `installationUpdate`) so the first reply has a valid service URL. Handler-presence gate returns 503 for message-typed activities pre-handler (rolling-restart safety). Two-phase idempotency: poisoned `tryBegin` returns 200 (terminal). `format()` is fail-closed `FormatResult` — non-text blocks surface `UNSUPPORTED_BLOCK` instead of silently dropping. | standalone |
| `@koi/channel-telegram` | Telegram `ChannelAdapter` — grammY polling and Bot API webhook deployments. Webhook mode requires `webhookSecret` (constant-time `X-Telegram-Bot-Api-Secret-Token` verification) AND atomic `claimWebhookUpdate` (the racy check-then-act `seenWebhookUpdate` was removed: two concurrent retries could both observe `seen=false` and fan out duplicate agent turns). Two-phase reserve/commit: `claimWebhookUpdate` ("claimed"/"duplicate"/"reclaimed" — `reclaimed` enables operator TTL/lease takeover for crashed workers) → multi-handler dispatch → `markWebhookProcessed`. Partial multi-handler success keeps the claim reserved (release would let retries duplicate the successful handler's side effects); `onWebhookCommitFailure` is the unified recovery hook for post-success commit failure, partial multi-handler success, and `releaseWebhookClaim` failure. `handleWebhook` fails closed with claim-release when no `onMessage` handler is registered yet (rolling-restart window). Outbound `sendOutbound` walks `ContentBlock[]` in caller order, batching contiguous text+button into one `sendMessage` group and flushing at every media boundary (`[text, image, text, button]` arrives as text → image → text+button, NOT all-images-then-all-text). Button `callback_data` encoder rejects `:` in `action` to avoid inbound delimiter collision and pre-validates ALL button payloads before any `sendPhoto`/`sendDocument` so bad payloads cannot orphan media. `TelegramPartialDeliveryError` for retry-unsafe partial multi-step send. Polling startup-window buffer + `ingressReady` gate prevents update loss between `bot.start` kickoff and middleware install. (closes #1362) | standalone |
| `@koi/channel-web` | Browser/HTTP `ChannelAdapter` — `Bun.serve` HTTP `POST /messages` + WebSocket `/ws` with strict per-thread routing. CSRF fail-closed at construction (`originAllowList` required when `authenticate` is set unless `allowAnyOrigin: true`). `Idempotency-Key` cache key is composite (principal + thread + endpoint) to prevent cross-tenant suppression; atomic reserve-before-await collapses concurrent retries to one dispatch and releases on 503. Streaming body cap (`readBodyWithCap`) defeats chunked / dishonest `Content-Length` attacks. Token comes from `Authorization: Bearer` for POST and from cookie `koi_ws` (preferred) or `?token=` (fallback) for `/ws` upgrades. `revokeSubscriptions(predicate)` synchronously prunes matched WebSockets so entitlement changes (logout, role downgrade, removed-from-thread) close affected sockets before the next outbound. Authenticated `POST /messages` requires `threadId` by default (no reply surface otherwise); hosts that want fire-and-forget ingestion opt in via `allowThreadlessAuthenticatedPost: true`. | standalone |
| `@koi/channel-whatsapp` | WhatsApp `ChannelAdapter` — Meta Cloud API webhook inbound (HMAC-SHA256 verify) + Graph API outbound (closes #1363). Composite `threadId = ${phoneNumberId}|${message.from}` so cross-business-number cross-talk is impossible; outbound `send()` throws `WRONG_BUSINESS_NUMBER` on threadId-vs-configured-pid mismatch. `metadata.recipientPhone` preserves the raw phone number for downstream consumers. Envelope-shape-drift detection: malformed Meta payloads (missing `entry`/`changes`/`value`/`messages`) and all-invalid-message batches surface via `onIngressIssue` so operators see schema regressions instead of silent drops; phone-number-mismatch issue carries the full `WhatsAppMessage` payload for diagnostics. Production-mode (`production: true`) requires an `onIngressIssue` callback at construction. Two-phase idempotency with poisoned `tryBegin` → continue (200-ack terminal, no redelivery). Connected + handlerRef gate prevents inbound dispatch before the handler is registered. | standalone |
| `@koi/daemon` | OS-process worker supervisor — pool with `maxWorkers` cap, restart policy (permanent/transient/temporary) with cancellable backoff, deadline-bounded terminate→kill teardown, watch-fault recovery with quarantine on failed teardown, subprocess backend with TTY-safe stdio and fast-exit event retention, `registerSignalHandlers` for SIGTERM/SIGINT graceful shutdown (one-shot, exit-aware, second-signal force-exit). Fails closed on un-killable quarantined workers so shutdown cannot silently orphan processes (#1338). Also ships the file-backed `BackgroundSessionRegistry` (one JSON per workerId, monotonic-version CAS, per-record cross-process lockfile with ownership-verified commit, path-traversal guard, `0o600`/`0o700` perms, atomic tmp+rename on register+update) + `attachRegistry(supervisor, registry)` bridge that mirrors every lifecycle event into the on-disk store, refreshes pid/startedAt on restart, and demotes `crashed` to `exited` when an off-path killer pre-claimed `terminating` (#1340). `@koi/daemon` — worker heartbeat IPC protocol (opt-in via `backendHints.heartbeat: true`), supervisor health snapshot (`supervisor.health()`), missed-heartbeat timeout detection. Heartbeats only detect hangs in subprocess workers today; other backends are unchanged. (#1341) | `daemon-supervisor`, `daemon-subprocess` |
| `@koi/decision-ledger` | Per-session decision ledger projection — read-only join over the runtime's `trajectoryStore` + optional `AuditSink.query()` with `RunReport` as a sidecar summary. Exposes two separate lanes (`trajectorySteps` sorted by `stepIndex`, `auditEntries` sorted deterministically by `(timestamp, turnIndex, originalIndex)`) rather than a merged timeline — a wall-clock merge would be misleading without a shared causal key. Session-integrity filtering on audit records: mismatched-session entries are dropped, counted, and surfaced via dedicated `SourceStatus` variants (`present-with-leakage` for partial leaks, `integrity-violation` for all-foreign fetches). Trajectory lane always reports `present-unverified` because `RichTrajectoryStep` has no `sessionId` field and trust is store-authoritative; `trajectoryTrustModel` + `allLanesFieldVerified: false` literal fields prevent callers from mistaking the absence for verified cleanliness. Iterative report-tree walker with cycle guard and 100k-node hard limit protects against malformed/deep `childReports` graphs. `RuntimeHandle.createDecisionLedger` factory wires the live `trajectoryStore` and `config.reportStore` automatically, accepting optional audit/report sink overrides for ad-hoc incident tooling (#1469 Phase 2a) | standalone |
| `@koi/event-trace` | ATIF trajectory recording middleware — tool metadata allowlisted (#1499), system step `__koi` transport for lossless round-trip. System steps now carry `observation` for MW span response preservation. `decisionCorrelationId` allowlisted for outcome linkage (#1465) — request-only, validated, with pruned-ID diagnostic | all queries |
| `@koi/fs-local` | Local filesystem backend (read/write/edit/list) | `local-fs-read` |
| `@koi/fs-nexus` | Nexus-backed filesystem backend — startup failure diagnostics now capture full Python stderr with bounded time (3s) and size (256KiB) (#1743) | `nexus-fs-read` (optional) |
| `@koi/fs-scoped` | Scope-wrapping adapter for filesystem backends — wraps any `FileSystemBackend` to enforce a workspace root boundary. When a manifest declares both `root` and `mode` fields, `resolve-filesystem.ts` wraps the resolved backend in `createScopedBackend()` so all path arguments are validated and rebased against the declared root before being forwarded to the underlying backend. | standalone |
| `@koi/gateway` | WebSocket gateway (#1365) — `createGateway()` factory wiring auth handshake, sliding-window sequence deduplication, per-connection backpressure watermarks, pluggable `SessionStore`, and `Bun.serve()` transport. `send(agentId, sessionId, frame)` enforces agentId ownership via `connSessionCache` (O(1), no I/O) and returns `PERMISSION` on mismatch. Transport infrastructure only — no tools or middleware. See `docs/L2/gateway.md` for full API. | standalone |
| `@koi/gateway-canvas` | Canvas-surface HTTP gateway (#1367) — `createCanvas()` factory wires a tenant-scoped surface store + SSE fan-out + `Bun.serve()` HTTP listener. Mandatory authenticator (rejected at construction); blank `agentId` rejected at the boundary. Tenant-scoped storage namespace (`(ownerId, surfaceId)` keys with discriminator bytes prevent cross-tenant squat/probe and `agentId:""` aliasing of the ownerless slot). Epoch- generation- and version-aware ETag (`${instanceEpoch}-${gen}-${ver}-${hash}`) closes stale-If-Match windows across no-op PATCH (version bump), delete+recreate-with-identical-content (generation bump), and process restart (random per-store epoch); update bumps version on every successful call including no-ops. Per-tenant + global capacity caps (`maxSurfacesPerTenant` + `maxSurfaces`) — `RESOURCE_EXHAUSTED` returns 503 with `Retry-After: 30`. SSE: tenant-qualified stream keys, per-subscriber `onClose` hook (WeakMap) so DELETE terminates the underlying `ReadableStream` (no zombie 200s); pre-start byte-budgeted buffer (per-handshake `2 × maxBodyBytes` + per-server 64 MiB cap) bounds memory under reconnect storms; throwing/duplicate subscribers contained. Initial `snapshot` event includes full surface content when it fits the handshake budget (cheap raw-length preflight before any JSON.stringify/encode), falls back to metadata-only on overflow so reconnects can self-heal without an out-of-band GET. DELETE-during-handshake race fenced via `closedBeforeStart` flag honored in `start()`. Cache-safety: `Cache-Control: private, no-store` + `Vary: Authorization` on every response. Store backend errors mapped to retryable 503 (transient classifier honors `EXTERNAL`/`TIMEOUT`/`UNAVAILABLE`/`RESOURCE_EXHAUSTED`/`RATE_LIMIT`/`retryable:true`). Defensive `snapshotEntry` with `Object.freeze` + `structuredClone` on metadata at ingress and egress so callers cannot mutate stored CAS state behind the store. The package is exempt from cassette recording (`GOLDEN_QUERY_EXEMPT`) — like `@koi/gateway-http`/`@koi/gateway-webhook`, it is HTTP infrastructure that sits outside the agent loop with no tool or middleware surface to exercise via LLM replay. Two standalone golden queries cover surface-store generation/CAS contract and the SSE manager's subscribe/publish/close lifecycle. | standalone |
| `@koi/governance-core` | Policy gate + setpoint enforcement middleware (#1392). Fail-closed `gate()` runs controller `checkAll()` + `backend.evaluator.evaluate()` before every model/tool call; setpoint denies throw `RATE_LIMIT`, policy denies throw `PERMISSION`, and both emit compliance records. Internal: `ensureSessionAbort` uses an early-return pattern so TS narrows the return type to `AbortController` (no behaviour change). Post-call recording awaits controller completion so the next gate reads up-to-date state; accounting failures latch a degraded flag that denies subsequent calls until the middleware is rebuilt. Trust-boundary hardening: `costUsd` validated for `Number.isFinite && >= 0`, token counts validated the same way, cost-calc failure omits `costUsd` so controller per-token fallback pricing runs. Turn-count recording in `onAfterTurn` (defense-in-depth: throws AND latches to survive host `.catch(noop)` wrappers). Stream accounting accumulates incremental usage deltas, honors `error.usage` as authoritative, and terminal recording runs before yielding the terminal chunk so accounting errors surface on the normal control path. Tool outcome recording (`tool_success`/`tool_error`) drives the controller's `error_rate` variable. Compliance redaction strips tool-call input values (keeps `toolId` + `inputKeys`). **Observer mode** (gov-9): when `observerOnly: true`, all `controller.record(...)` sites (token_usage, turn, tool_success, tool_error) are no-ops — used when a host (e.g. CLI's engine-extension) already records via the same controller, preventing double-counting. **Stream containment hardening**: per-chunk validation flags malformed token usage before accumulation; all terminal paths (error/done/finally) latch the degraded flag if any malformed chunk was seen, even when terminal usage is well-formed; post-terminal chunks are dropped without yield to prevent late-arriving aggregator state. **Ternary verdict — ask flow (gov-11, #1878):** `GovernanceVerdict` supports `{ ok: "ask", prompt, askId, metadata? }` alongside allow/deny. Ask routing runs inside the existing `gate()` — no parallel interposition layer. Missing `TurnContext.requestApproval` fails closed to `PERMISSION`. Handler decisions map to `allow` (proceed once), `always-allow` session (record grant keyed by `computeGrantKey(kind, payload)` — identical subsequent asks skip the handler), `always-allow` permanent (grant + one-shot `onApprovalPersist(PersistentGrant)` — downstream coalesced waiters are idempotent), `deny`/`modify` (throw `PERMISSION`). Per-session `AbortController` plus inflight coalescing `Map<AskId, Promise<...>>` ensures `onSessionEnd` aborts every pending ask and drops the session's grant set. `approvalTimeoutMs` (default `DEFAULT_APPROVAL_TIMEOUT_MS = 60_000`) guards handler hangs via `withTimeout` → `ApprovalTimeoutError` re-thrown as `KoiRuntimeError("TIMEOUT", ...)` so hosts can distinguish "user took too long" from "user said no" (`PERMISSION`). Exhaustive `switch` over `ApprovalDecision` with `const _: never = decision` guard so any new decision kind is a type error, not a silent fail-open. | `governance-allow`, `governance-deny` |
| `@koi/governance-approval-tiers` | Persistent approval allowlist for `ok:"ask"` verdicts (gov-12, #1879). `createJsonlApprovalStore({ path })` appends each `always` grant as one JSON-Lines row under O_APPEND with explicit `0o600` open-mode (no chmod-after-write race), bounded row size (default 4 KiB), workspace-scoped grant key, and optional alias canonicalization. `wrapBackendWithPersistedAllowlist(backend, store, { resolveAgentId })` short-circuits subsequent `ok:"ask"` verdicts to allow on a cached match — fail-closed if the store throws. `createPersistSink(store, opts)` is the recommended `onApprovalPersist` callback; `createViolationAuditAdapter({ store, onViolation, resolveAgentId })` wraps it to also emit a synthetic `approval.persisted` info-violation through the host's audit channel only after a confirmed durable write. Scope key shape: `${hostId}:${cwdHash}:${actor}` where `actor = hostId` for the host agent (stable across restarts) and `actor = pid.id` for sub-agents (session-only isolation). | `governance-allow` (after persist) |
| `@koi/governance-delegation` | Capability tokens + delegation chains (#1395). L2 implementation of `@koi/core`'s `CapabilityVerifier` / `CapabilityRevocationRegistry` / `CapabilityTokenStore` contracts. `issueRootCapability` / `delegateCapability` produce HMAC-SHA256 or Ed25519-signed `CapabilityToken`s; `createCapabilityVerifier` validates signature, expiry, session, revocation, chain attenuation (`isPermissionSubsetWithAsk` enforces ask preservation), `maxChainDepth`, resource subset, and bound parent lookup. `createGlobScopeChecker` fail-closes on resource-scoped tokens, `permissions.ask` matches, `group:*` patterns, and PoP-required tokens. Ed25519 trust split: required `issuerKeys` (per-token AgentId binding) plus separate `rootKeys` allowlist (chainDepth=0 authority). Snapshot-via-`structuredClone` + null-prototype permission containers defend against caller-mutation TOCTOU and `Object.prototype` pollution. `createMemoryCapabilityRevocationRegistry` is the in-memory revocation store + token store with ancestor-aware cascade (out-of-order parent registration also propagates). | `governance-delegation-issue`, `governance-delegation-verify` |
| `@koi/governance-defaults` | Out-of-box `GovernanceController` + `GovernanceBackend` + pricing table for `createRuntime({ governance: withGovernanceDefaults(...) })` (#1874). `createInMemoryController` ships all 10 `GOVERNANCE_VARIABLES` with `>=`-at-limit semantics, per-variable retryable flags, `run_reset`/`session_reset` handling with `boundaryTimestamp` clamping (deprecated `iteration_reset` alias accepted for backward compat), `turn_refund` rollback support (`turn_count` decrements clamped at zero), and `setContextOccupancy()` for host-driven context-pressure telemetry. Fail-closed input handling: NaN/negative/Infinity token fields dropped, non-number config limits rejected at construction, cross-field invariant `errorRateMinSamples <= errorRateWindow` enforced. `forge_depth` is now a concurrent counter paired with `forge_release` (PR #1925) — mirrors `spawn` / `spawn_release` discipline; `forge_budget` remains the cumulative forge-count sensor and is the correct sensor for a lifetime cap once hosts start emitting paired releases. `createPatternBackend` does last-match-wins rule evaluation with kind-scoped `toolId`/`model` selectors and `schema.invalid` fail-closed on malformed payloads (null/array/non-string); rule descriptors are exposed via `backend.describeRules()` so hosts can render the active policy in operator UIs. `DEFAULT_PRICING` is deep-frozen; `withGovernanceDefaults` seeds per-token fallback pricing from `claude-sonnet-4-6` (caller's pricing table first, shipped `DEFAULT_PRICING` as floor) so the spend cap keeps advancing when `cost.calculate()` throws for an unknown model alias. Structural compatibility with `GovernanceMiddlewareConfig` — no L2→L2 runtime dep on `@koi/governance-core`. | `governance-defaults-*` |
| `@koi/hook-prompt` | Prompt hook executor — single-shot LLM verdict parsing with hardened JSON extraction, string-boolean coercion, and denial language detection (#1309) | standalone |
| `@koi/hooks` | Sole hook dispatcher (command/HTTP/prompt/agent) — single dispatcher + observer tap pattern (#1513). Hook policy tiers (#1282): hooks tagged as `managed`/`user`/`session` with tier-phased dispatch (managed → user → session) and `HookPolicy` filtering (`disableAllHooks`, `managedOnly`, `allowUserHooks`, `allowSessionHooks`). `RegisteredHook` with stable `${tier}:${name}` IDs. Two loader entrypoints: `loadRegisteredHooks()` (strict, all-or-nothing) and `loadRegisteredHooksPerEntry()` (#1781: one bad entry does not nuke valid peers; returns `HookLoadError[]` with `kind` discriminator and `failClosed` sniffed from raw JSON so hosts can fail-close per entry). `createHookMiddleware` is the sole authority for all hook events; `@koi/runtime`'s `createHookObserver` subscribes via `onExecuted` tap for ATIF recording. Per-call abort propagation, fail-closed pre-hook blocking, cancel-redaction with `failClosed` filtering. Prompt hooks wired via `PromptExecutorAdapter` with per-session token budgeting and payload size capping (#1309). DNS-level SSRF guard for HTTP hooks with IP validation, IP pinning, header injection prevention, and bounded response body (#1278, #1279) | `tool-use`, `hook-blocked`, `hook-once` |
| `@koi/mcp` | MCP transport + tool/resource resolver. OAuth 2.1 authorization code + PKCE for HTTP servers, RFC 9728/8414 discovery, RFC 7591 DCR with RFC 7592 rollback (#1296), RFC 8707 `resource` indicator (opt-out via `includeResourceParameter: false`), CAS-on-UUID `generation` for stale DCR invalidation, tenant-scoped storage (4-tuple key), `OAuthRuntime` injection, structured `OAuthFailureReason` via `onAuthFailure`, `AUTH_REQUIRED` mid-session re-auth, `@koi/secure-storage` keychain token persistence (#1633) | `mcp-tool-use` |
| `@koi/ipc-local` | In-process mailbox IPC — `MailboxComponent` backed by in-memory storage with microtask dispatch and `MailboxRouter` for authenticated multi-agent routing. Used by `@koi/mcp-server` for platform mailbox operations. | standalone |
| `@koi/mcp-server` | MCP server — exposes agent tools + platform capabilities (mailbox, tasks, registry) to external MCP clients. 8 platform tools built as real Koi Tools via `buildTool()` (added `koi_list_mailbox` for header-only inbox inspection). Security: callerId enforcement, event-only mailbox, owned task mutations, visibility-filtered registry, prototype pollution protection, error sanitization | `mcp-server-send` |
| `@koi/memory` | Memory recall, scoring, and formatting — static headings, JSON metadata inside `<memory-data>` trust boundary, scan resilience (`starved`, `candidateLimitHit`), opt-in `maxCandidates` I/O bound | `memory-recall` |
| `@koi/memory-fs` | File-based memory storage backend — per-dir mutex + `.memory.lock` for write serialization, worktree-local by default (`shared: true` opt-in with policy pinning), atomic temp-rename updates, `indexError` on mutation returns, serialized MEMORY.md rebuilds. **Atomic `upsert(input, {force})`** runs name+type lookup + Jaccard dedup + write/update inside a single `withDirLock()` critical section (closes the read-list-write TOCTOU window). Returns discriminated `UpsertResult`: `created` / `updated` / `conflict` / `skipped` | standalone |
| `@koi/memory-tools` | Memory read/write/list tools — sandboxed with `memoryDir` filesystem caps, atomic `storeWithDedup`, idempotent delete, SkillComponent behavioral guidance. Tool output excludes internal `filePath` (#1725). **Type classification guidance (#1964):** `memory_store` schema `type` field now carries per-type decision rules with examples to prevent LLM misclassification (`feedback` vs `project`, personal contacts → `user` not `reference`). | `memory-store` |
| `@koi/middleware-memory-recall` | Frozen snapshot + live delta memory recall — scans `.koi/memory/` once per session (salience-scored, 8K budget), then per-turn appends a live delta for new/modified memories (4K default budget) via cheap `list()` fingerprint + FNV-1a content signatures. Delta inserted before the last user turn to preserve transcript semantics. Optional per-turn relevance selector uses opaque record IDs. Per-session state isolation | `memory-recall` |
| `@koi/memory-team-sync` | Team memory sync safety boundary — type filtering (`user` always denied), secret scanning via `@koi/redaction`, fail-closed stub with deferred transport. **Email backstop (#1964):** `reference` memories containing email addresses are blocked to catch contact misclassification; SSH git remotes excluded via `(?!:)` lookahead. `feedback`/`project` intentionally not email-blocked to preserve shared operational aliases. | standalone |
| `@koi/middleware-audit` | Security-grade compliance logging (#1627) — intercepts all 6 event categories (`model_call`, `tool_call`, `session_start`, `session_end`, `permission_decision`, `config_change`). Bounded ring-buffer backpressure queue (drop-oldest on overflow), SHA-256 hash-chain tamper detection, Ed25519 per-record signing with gap markers (GENESIS_HASH on chain restart makes overflow detectable), struct-level redaction via `@koi/redaction`. `onPermissionDecision` fires via `dispatchApprovalOutcome` callback BEFORE `next(request)`. **schema_version bumped to 2 (#2009, #1393):** `tool_call` entries now carry a top-level `toolName` field (promoted from `request.toolId`) for O(1) query-time filtering; the `kind` union adds `compliance_event` (emitted via `ComplianceRecorder` from `@koi/governance-defaults` when governance fires a violation). All producers (`middleware-audit`, `middleware-permissions`, compliance recorder) emit v2; v1 readers exhaustively switching on `kind` gate on `schema_version` before accepting. | standalone |
| `@koi/audit-sink-ndjson` | Buffered NDJSON file sink — one JSON object per line, single Bun write stream per instance, periodic flush interval. Fail-closed `getEntries()`: any parse failure throws (no silent record loss) (#1627). **Rotation (#1992):** opt-in size- and daily-rotation via `NdjsonRotationConfig`; active file archived to `<filePath>.archive/` with monotonic `<seq>-<uuid>.ndjson` filenames (clock-skew safe). `getEntries()`/`query()` transparently span archives + current file so hash-chain verification works across rotation boundaries. | standalone |
| `@koi/audit-sink-nexus` | Nexus-backed `AuditSink` — batched writes (32 entries or 5 s, whichever comes first) to NDJSON files under a configurable Nexus path prefix, daily filename rotation, and `query(sessionId)` via Nexus grep. Fail-closed on flush errors: entries that cannot be written are dropped with a structured stderr log rather than silently swallowed. Shares the `@koi/nexus-client` HTTP transport. (#1399) | standalone |
| `@koi/audit-sink-sqlite` | SQLite audit sink with WAL mode, composite `(timestamp, kind)` index, `query(sessionId)` (#1627). Schema migration adds `canonical_json` column idempotently — `PRAGMA table_info` probes before `ALTER TABLE ADD COLUMN`, re-checks on duplicate-column error, and rethrows only on genuine migration failure. **Retention pruning (#1992):** opt-in `SqliteRetentionConfig` deletes rows older than `maxAgeDays` on init and on a configurable interval (default 1 hr), runs `VACUUM` after each pass; pruning is scoped to `(agent_id, session_id)` and only fires for sessions with a `session_end` and no chain follower so cross-agent records and crashed/in-flight sessions are preserved. |
| `@koi/violation-store-sqlite` | SQLite `ViolationStore` (#1393) — durable governance violation persistence with WAL mode + `PRAGMA busy_timeout = 5000`. Buffered appends (default 100 entries / 2 s flush), `MAX_BUFFER_BACKLOG = 10_000` oldest-drop cap on sustained flush failures. `record()` pre-serializes `violation.context` to quarantine poison rows (BigInt / circular) — one bad context becomes `context=null`, healthy siblings still land. Idempotent `close()` returns `{ droppedCount }` so callers can fail-closed on loss; `CLOSE_FLUSH_ATTEMPTS = 3` retries on transient errors. Auto-wired by `@koi/cli` to `~/.koi/violations.db` when governance is enabled; explicit `--violation-sqlite="<path>"` fails closed on write errors, default path degrades with a warning. | standalone |
| `@koi/middleware-exfiltration-guard` | Secret exfiltration prevention — default-on for adapters with terminals. Scans tool I/O (input + output), model output (streaming + non-streaming including `richContent`), buffers all content-bearing stream chunks. `RuntimeConfig.exfiltrationGuard` to configure/disable | `exfiltration-guard-block` |
| `@koi/middleware-extraction` | Post-turn learning extraction — intercepts spawn-family tool outputs, runs regex + LLM extraction, persists via `MemoryComponent.store()` with `CollectiveMemoryCategory` metadata. Secret-scanned (before logging), session-scoped, namespace-isolated. **Fix (#1964):** `heuristic`/`pattern` → `feedback`. **Fix (#1966):** `MemoryType` now threaded via `MemoryStoreOptions.type`; JSON output pre-filtered through allowlist before extraction; command-result envelopes skipped. | standalone |
| `@koi/middleware-collective-memory` | Cross-run learning persistence — extracts reusable learnings from spawn-family tool outputs (marker regex + 4 heuristic patterns), persists them as `CollectiveMemory` on brick artifacts via `ForgeStore.update()` with CAS, and injects relevant entries into future model calls as a system message (one-shot per session, token-budgeted). Post-session LLM extraction batches up to 20 spawn outputs for structured JSON parsing. Compaction via cold-age eviction when entries exceed threshold (default 50) or token budget (default 8K). Priority 305 — fires after permissions, before memory-recall | standalone |
| `@koi/middleware-dream` | Background dream consolidation gate — `onSessionEnd` increments `<memoryDir>/.dream-gate.json` via atomic `mutateGateState` (in-process mutex chain + cross-process O_EXCL on `.dream-gate.lock` with stale eviction). When the gate fires (default: 5 sessions and 24 h since last dream), a fire-and-forget `runDreamConsolidation` runs under a PID-bearing `.dream.lock`. Monotonic post-success update preserves session-end events that arrived during consolidation. Wired only when `ctx.modelAdapter` is present | standalone |
| `@koi/middleware-planning` | Structured multi-step plan tracking via the `koi_plan_write` tool (#1836). Opt-in via `KOI_PLANNING_ENABLED=true` (auto-paired with `@koi/middleware-plan-persist`). Per-turn auth, epoch-based session-recycle isolation, teardown abort signal, `PlanUpdateContext.commitToken` for CAS | standalone |
| `@koi/middleware-plan-persist` | File-backed persistence layered on `@koi/middleware-planning` (#1842). Hooks `OnPlanUpdate` to mirror every successful `koi_plan_write` to `<baseDir>/_active/<sha256(sessionId)>.md` via exclusive-link commit; exposes `koi_plan_save` (writes named `<ts>-<slug>.md` checkpoints) and `koi_plan_load` (reads named checkpoints; refuses `_active/` paths to prevent cross-session disclosure). Per-session epoch CAS rejects stale-incarnation writes; per-session serial write chain orders concurrent saves; `clearJournal(sessionId)` for explicit `/clear` reset; `restoreFromJournal(sessionId)` returns a structured `RestoreJournalResult` distinguishing not-found / io-error / corrupt | `plan-persist-flow` |
| `@koi/middleware-permissions` | Tool/model permission gating middleware — approval trajectory capture via `approvalStepHandle` dispatch relay (#1498). `onPermissionDecision` hook dispatched via `dispatchApprovalOutcome` callback BEFORE tool execution (decoupled from tool success/failure, #1627) | `permission-deny`, `denial-escalation` |
| `@koi/middleware-report` | RunReport generation middleware | `tool-use` |
| `@koi/middleware-tool-disclosure` | Progressive tool disclosure (#2099) — above a configurable threshold, swaps full `ToolDescriptor[]` for compact summaries in the model request and lifts tools back to full descriptors on demand via the `promote_tools` companion tool. Two-step authorization separates the promoted set (Map) from the executable set (Set, rebuilt at `wrapModelCall`) so a same-turn promote+execute cannot bypass disclosure; companion stale-turn guard binds promotions to `lastTurnId`. Fingerprint includes `origin` + `server` to detect descriptor swaps; duplicate-name fail-closed in both threshold branches. | `tool-disclosure` |
| `@koi/middleware-fs-rollback` | Snapshot/restore working tree around protected tool calls (#1421, sub-task 3) — wraps `fs_write`/`fs_edit` (or any configured tool) with `git stash push --include-untracked --message <token>`, then `git stash pop` on tool failure / `git stash drop` on success. Priority 180 (innermost of `@koi/middleware-tool-error-formatter` at 170 so rollback restores BEFORE the formatter wraps the throw). No-ops outside a git repo (warns once) and for paths outside the resolved git root. Tracks per-call whether a stash was actually created (clean tree → no entry → no pop attempt). Pop conflicts surface as `KoiError(INTERNAL, retryable=false)` with `context.stashRef` for manual recovery. Opt-in via manifest — not auto-instantiated by the CLI. See `docs/L2/middleware-fs-rollback.md`. | `fs-rollback` |
| `@koi/middleware-tool-error-formatter` | Format tool errors into actionable model feedback (#2099) — catches errors via `wrapToolCall`, formats them into model-readable strings, returns as `ToolResponse` instead of throwing. Pluggable formatter with default fallback. Defense-in-depth secret redaction (`sk-…`, `Bearer`, `Basic`, `Cookie`, JWT, AWS, GitHub, Slack, Stripe, Google, conn-strings) plus key-aware redaction for sensitive keys (password/token/secret/authorization/cookie/api_key). Depth-bounded sanitize (`MAX_SANITIZE_DEPTH=64`). Symbol provenance markers (`GUARDRAIL_PROVENANCE`, `COMMITTED_PROVENANCE`, `INTERNAL_PROVENANCE`) for unforgeable rethrow signaling; backwards-compatible boolean markers retained. Configurable `passthroughCodes` and `passthroughPredicate` so guardrails wired inside the formatter can re-throw. | `tool-error-formatter` |
| `@koi/middleware-task-anchor` | CC-parity `<system-reminder>` injection that re-anchors the model on the live task board after `K` idle turns with no task-tool activity (#1837). Priority-ordered render (in_progress → pending → failed → killed → completed), hard cap with per-status overflow counts, stop-gate retry rollback, hook-blocked/`{ ok: false }` task-tool failure detection. Gated on successful task-tool engagement so shell-only sessions never receive unsolicited nudges. Emits `ctx.reportDecision` on every observed board — `{ action: "inject", reminderKind, forced, idle, taskCount }` or `{ action: "suppress", reason: "forceRequiresTasks"\|"nudgeDisabled"\|"noPriorTaskTool", boardState: "empty" }` (#2015); `reportDecision` failures are swallowed via `swallowError` so telemetry faults cannot abort the model turn | `task-anchor-reminder` |
| `@koi/middleware-semantic-retry` | Semantic retry on model failures | standalone |
| `@koi/middleware-strict-agentic` | Stop-gate that blocks premature completion on filler/plan-only turns (#1708). Positive-match filler regex (first-person-future: "I will", "let me <verb>", etc.); trailing-? user-question exemption; isExplicitDone with negation + forward-work rejection; blank-output guard. Per-run state keyed by `runId` (engine mints different turnIds for stream vs gate); eager cleanup on continue/breaker-release. Circuit breaker trips on `blocks > maxFillerRetries` (default 2) — emits `strict-agentic:circuit-broken` via `ctx.reportDecision`. Opt-in per agent; not auto-installed by the CLI | `strict-agentic` |
| `@koi/dream` | Offline memory consolidation — merges similar memories by Jaccard similarity (complete-linkage, type-partitioned), prunes cold records, write-ahead LLM merge with supersedes provenance. Called by scheduler/daemon | standalone |
| `@koi/session` | Session persistence (SQLite/WAL) + JSONL transcript middleware + `resumeFromTranscript()` for crash recovery. Phase 2e-2: `SessionStatus`, `ContentReplacement`, `CompactResult` boundary extension, instance-local queue isolation. Engine-injected `system:*` senderIds preserved across persist/resume cycle. **Transcript pollution guard (#1417):** inbound messages whose `senderId` starts with `system:internal:` are skipped on commit so middleware-injected scratch (e.g., `system:internal:verifier` revise feedback, `system:internal:verifier-replay` prior-turn replay) drives in-flight model behavior without corrupting the durable transcript or resume context | `session-persist`, `session-resume`, `system-sender-resume` |
| `@koi/middleware-output-verifier` | Two-stage output quality gate (#1417, priority 385). Stage 1: deterministic checks (sync). Stage 2: optional LLM-as-judge (async, sampled). Three actions per check: `block` / `warn` / `revise`. The revise loop injects feedback as `senderId="system:internal:verifier"` (request) and `"system:internal:verifier-replay"` (prior assistant output) — both prefixes that `@koi/session`'s transcript skips. Streaming: fail-closed before first yield, degrades to warn after first yield. Judge prompt hardened against injection (delimited sentinels stripped from candidate body). Per-session rubric override via `setRubric(sessionId, rubric)` so concurrent tenants cannot bleed criteria; default rubric captured at construction is immutable; `onSessionEnd` clears the completed session's override in long-lived runtimes | standalone (2 golden queries) |
| `@koi/middleware-event-rules` | Declarative YAML event→action rules (#1422, priority 50, intercept phase). Events: `tool_call`, `turn_complete`, `session_start`/`session_end`. Actions: `skip_tool` (per-session block, half-open circuit-breaker on windowed conditions), `log`, `notify`, `emit`, `escalate`. ReDoS-hardened predicate compiler (256-char pattern cap, 4096-char input cap, nested-quantifier rejection); Damerau-Levenshtein-1 typo guard on `tool_call` core fields. Counter partitioning by `(rule.name, toolId)` so a multi-tool rule can't quarantine tool B from tool A's failures. `skip_tool` validation rejects rules whose action toolId is unreachable from `match.toolId`, and rejects narrower-than-toolId predicates (input fields, agentId, sessionId, turnIndex). Side-effecting actions are fire-and-forget but tracked per-session and drained on `onSessionEnd` (5s `runBounded` ceiling per handler). Per-session generation token fences `onSessionEnd` teardown when a sessionId is reused mid-drain. Manifest factory restricts ruleset to `log` actions only (security-trusted) | standalone (2 golden queries) |
| `@koi/middleware-degenerate` | Multi-variant tool dispatch with fitness-ranked failover (#1417, priority 1000 — innermost tool MW). Pool build at session start: ranks all bricks by fitness, walks the full ranked list to fill `maxVariants` USABLE entries (backfills past broken top-ranked bricks), rejects duplicate aliases (within and across capabilities), and enforces an identical `brick.policy` envelope across every variant — divergent sandbox/network/permission would silently widen the alias's effective trust scope under failover. Dispatch preserves the caller-addressed alias as `request.toolId` so outer auth/audit MW see one identity per pool; variant identity flows via `metadata.selectedVariantId`. Strategies: `fitness`, `thompson`, `round-robin`. Attempt log capped at 256 entries (O(1) amortized) | standalone (2 golden queries) |
| `@koi/scratchpad-local` | In-process key-value scratchpad for agent working memory — path-namespaced entries with byte-budget enforcement, optional TTL with background sweep, CAS conflict detection via `expectedGeneration`, reuseToken for shared access across sub-agents in the same group. Tokenless and token-bearing stores are isolated: opening a tokenless store where a token-bearing one exists (or vice versa) fails closed. TTL-expired entries reclaim their byte budget before accepting a new write to prevent double-counting (#1370) | standalone |
| `@koi/workspace` | Git-worktree-backed workspace backend — `createGitWorktreeBackend` allocates dedicated worktrees per agent, exposes `findByAgentId` for crash-survivor discovery, `exists()` for authoritative liveness checks, and `isHealthy()` for branch-drift detection. `createWorkspaceProvider` (ComponentProvider) manages the full lifecycle: attach creates/reuses a worktree, detach disposes it per `CleanupPolicy` (`always`/`never`/`on_success`). Crash-survivor reuse requires sandboxed backends with verifiable setup attestation; unsandboxed backends always dispose-and-recreate. `tryDispose` NOT_FOUND is confirmed-absent on sandboxed or no-`exists()` backends; on unsandboxed backends with `exists()`, it defers to the oracle (#1370) | standalone |
| `@koi/outcome-evaluator` | LLM-as-judge rubric iteration loop — evaluates agent output against a structured rubric using a separate grader model call, re-prompts with structured per-criterion feedback until all required criteria pass or the iteration budget is exhausted. Circuit breaker prevents consecutive-identical-failure infinite loops. Optional `isolateCriteria` mode prevents halo effects between criteria | standalone |
| `@koi/model-router` | Multi-provider LLM routing with fallback chain, circuit breaker, health probe, and latency tracking. Emits `router.*` decisions (selected target, attempted targets, fallback_occurred, latency_ms) into ATIF MW spans via `ctx.reportDecision` | `model-router` |
| `@koi/model-openai-compat` | OpenAI-compatible model adapter (OpenRouter etc.). `supportsReasoning` enables `reasoning: { effort }`. New flags: `supportsReasoningExclude` (`reasoning: { exclude: true }`), `supportsThinkingType` (`thinking: { type: "summarized" }`), `thinkingDisplay` (`"full"`/`"summarized"`/`"hidden"`). Buffered tool streaming (`supportsToolStreaming: false`) replays post-tool text/thinking as `text_delta`/`thinking_delta` at finish time. SSE errors deferred past `finish()` flush so tool calls always precede terminal errors. | all LLM queries |
| `@koi/permissions` | Permission backend (bypass/default/nexus modes) | `permission-deny` |
| `@koi/permissions-nexus` | Local-first Nexus-backed `PermissionBackend` (#1399) — every `isAllowed()` check is answered from an in-memory local store (zero network hops on the hot path); writes propagate to Nexus asynchronously. Includes `NexusRevocationRegistry` (polling-based remote revocation with configurable interval) and `NexusDelegationHooks` (store delegation grants on Nexus for cross-node visibility). Session reset clears the in-memory grant set and resumes polling. Fail-closed: if the Nexus write-through throws, the permission decision is still enforced locally. | standalone |
| `@koi/nexus-delegation` | Nexus-backed `DelegationComponent` (#1473) — mints a per-child Nexus API key on every spawn, attenuates capability scope from parent to child, and revokes the key when the child terminates. `createNexusDelegationBackend` owns local grant store, TTL verify cache, retry queue for transient revoke failures, and tombstone tracking for verify-vs-revoke races. Pre-POST drain serialization and bounded dispose-time revoke (`REVOKE_DISPOSE_TIMEOUT_MS = 5000`) prevent host teardown from completing with a per-child key still active. `createNexusDelegationProvider` wires into Agent assembly under the `DELEGATION` token; `@koi-agent/cli` instantiates it when manifests declare `delegation.backend: "nexus"`. | `nexus-delegation-grant`, `nexus-delegation-revoke` |
| `@koi/plugins` | Plugin manifest validation, multi-source discovery (managed > user > bundled), and in-memory registry conforming to `Resolver<PluginMeta, LoadedPlugin>`. Fail-closed shadowing, path containment via `realpath()`, TOCTOU guards on load, availability gating. Directory-name === manifest-name invariant enforced (#1342). Lifecycle CRUD: `installPlugin`, `removePlugin`, `enablePlugin`, `disablePlugin`, `updatePlugin`, `listPlugins` with rollback-safe updates, crash recovery (`recoverOrphanedUpdates`), name validation, and `createGatedRegistry` for runtime enable/disable enforcement. Session activation wires plugin skills/hooks/MCP into TUI and CLI start (#1345). Security hardening (#1621): gated registry fails closed on corrupt state, symlink dereferencing on install/update | `plugin-validate`, `plugin-lifecycle` |
| `@koi/query-engine` | Model stream consumer + turn runner; `validateToolArgs` recognizes `items`/`properties`/`required` plus constraint keywords. `runTurn` deduplicates identical tool calls within a single model response (#1580). Doom loop detection (#1593): per-key streak counters with configurable threshold/intervention budget, mixed-turn filtering, `system:doom-loop` privileged intervention messages. Tool argument type coercion (#1611): shallow string→number/boolean coercion before validation, consistent coerced-args keying across dedup/doom-loop/execution. `runTurn` now emits a separate `tool_result` event after each tool finishes executing (#1583) — distinct from `tool_call_end` (which carries `AccumulatedToolCall` arg metadata). Output collectors and the TUI tool-block reducer key off `tool_result.output` for the actual execution output | all queries |
| `@koi/scheduler` | In-process task scheduler — delayed dispatch, cron schedules, retry with exponential backoff + jitter, dead-letter queue, observable `SchedulerEvent` stream. SQLite-backed persistence via `createSqliteTaskStore` / `createSqliteScheduleStore` / `createSqliteRunStore`. `createSchedulerComponent` wraps into ECS `SchedulerComponent` singleton. Poll loop (default 1 s) with min-heap early-exit; `TaskDispatcher` receives `AbortSignal` for timeout enforcement. Optional distributed mode (#1390): pass `{ queueBackend, nodeId }` to swap the local heap for `pollDistributed()` claim/ack/tick over any `TaskQueueBackend`. (#1384, #1390) | `scheduler-tools` |
| `@koi/scheduler-provider` | 9 LLM-callable scheduler tools with agentId pinning — `scheduler_submit`, `scheduler_cancel`, `scheduler_schedule`, `scheduler_unschedule`, `scheduler_pause`, `scheduler_resume`, `scheduler_query`, `scheduler_stats`, `scheduler_history`. `createSchedulerProvider(component)` returns `Tool[]`; each tool validates with Zod and scopes results to the calling agent's identity. Attaches `SCHEDULER` ECS token for downstream ECS access. (#1384) | `scheduler-tools` |
| `@koi/scheduler-nexus` | Nexus-backed `TaskStore` + `ScheduleStore` + `TaskQueueBackend` for distributed scheduling (#1390). `createNexusSchedulerBackends(config)` returns the trio ready to plug into `createScheduler(...)` distributed-mode args. Cross-node `claim`/`ack`/`tick` with visibility-timeout-driven re-claim; cron submissions are deduped across nodes via per-minute tick keys. CLI scheduler preset auto-migrates local SQLite stores into Nexus on first activation when `schedulerNexus` host config is present (running tasks requeued with a consumed retry; exhausted ones dead-lettered). Standalone golden coverage in `golden-replay.test.ts`. | standalone |
| `@koi/harness-scheduler` | Auto-resume scheduler for suspended harnesses (#1390). `createHarnessScheduler({ harness, backoff })` polls a `SchedulableHarness` (duck-typed: `status()` + `resume()`), resuming when the harness reports `suspended` and stopping after the configured retry budget. Tracks `totalResumes` / `lastError` on `status()`. Pure L0 deps; no LLM-callable tools. | standalone |
| `@koi/spawn-tools` | Agent spawn tool + coordinator utilities (TaskCascade, recoverOrphanedTasks). `agent_spawn.context` is forwarded as structured `SpawnRequest.context` and mirrored into the child description for compatibility with description-only spawn implementations. Idempotent retry-dedup (#1709): when `context.task_id` is set and a `SpawnResultCache` is provided, repeat calls with the same `(parentAgentId, agentName, task_id, request-digest)` return the cached output with `deduplicated:true` instead of launching a duplicate child. `RuntimeHandle.spawnResultCache` is always populated — defaults to a 256-entry LRU; pass an explicit instance via `RuntimeConfig.spawnResultCache` to share across runtimes. Concurrent retries coalesce on a single in-flight spawn; failures are not cached; `cacheable:false` placeholders (deferred / on-demand delivery) skip the LRU so retries can recover the real output | `spawn-tools` |
| `@koi/task-tools` | Task board tools (create/get/update/list/stop/output/delegate) + ComponentProvider + offset-based output reads. `task_update(completed)` defaults `output` to `"Completed: <subject>"` when omitted (#1785) | `task-tools` |
| `@koi/toolsets` | Named composable tool presets — `resolveToolset`, `createBuiltinRegistry`, `mergeRegistries`, `resolutionToToolAllowlist`. Four built-in presets: `safe` (read-only web + fs), `developer` (mode:all), `researcher` (safe + ToolSearch), `minimal` (AskUserQuestion only). Wildcard inheritance always rejected; registry deep-frozen; fail-closed merge by default (#1827) | standalone |
| `@koi/temporal` | Durable agent execution via Temporal — `createTemporalScheduler` (TaskScheduler over Workflows + Schedules with idempotent submit, ownership-verified cancel, memo-backed reconciliation), `createTemporalSpawnLedger` (semaphore-backed SpawnLedger), `createTemporalWorker` (worker factory with lifecycle-safe dispose). Infrastructure L2 — no LLM-callable tools | standalone |
| `@koi/tasks` | Task board stores + runtime task system (output streaming, task kinds, registry, runner, LocalShellTask lifecycle). `ManagedTaskBoardConfig` gains `onEngineEvent` + `agentId` for automatic plan/progress engine event bridging (#1555). Task kind validation via `isValidTaskKindName()`, unsupported lifecycle stubs with `registerDefaultLifecycles()`, atomic `killIfPending()` for unsupported-kind rejection (#1242) | `task-board` |
| `@koi/bash-ast` | AST-based bash classifier (L0u, PR #1660) — tree-sitter-bash walker extracts trustworthy `argv[]` from simple commands, fails closed on unknown grammar, transitional regex fallback via `@koi/bash-security` for `too-complex` outcomes until #1622 ships three-state permissions. Hard-denies escape-related `too-complex` (backslash in word/string, backslash-newline line continuation) instead of falling through. One-time async `initializeBashAst()` with cached promise; rejection resets cache for retry. `matchSimpleCommand()` pure matcher against `BashRulePattern` (L0 contract). WASM grammar vendored at `vendor/tree-sitter-bash.wasm` | `bash-exec`, `bash-ast-too-complex` |
| `@koi/bash-security` | Prefilter + transitional regex fallback for `@koi/bash-ast` (L0u) — byte-level injection/path/command classifiers exposed to bash-ast's two-phase prefilter, TTP denylist remains for the `too-complex` compatibility branch until #1622. Will shed the TTP classifier together with the fallback branch in the #1622 follow-up | standalone |
| `@koi/tools-bash` | Bash execution tool: cwd-contained, env-isolated, process-group kill, exfiltration denylist. Now imports `classifyBashCommand` + `initializeBashAst` from `@koi/bash-ast` (PR #1660) instead of `@koi/bash-security` — both `createBashTool` and `createBashBackgroundTool` await `initializeBashAst()` at the top of `execute()` before the sync classifier reads the cached parser. Also includes `createBashBackgroundTool()` for fire-and-forget subprocess execution via `ManagedTaskBoard`, shared `exec.ts` spawn/drain layer, `trackCwd` sentinel for directory tracking, OS sandbox DI (`sandboxAdapter` + `sandboxProfile`), and `sandboxed` boolean on `BashSuccessResult` indicating OS sandbox confinement. Foreground `Bash` validates per-call `timeoutMs` and clamps it to `BashPolicy.maxTimeoutMs` (default 5 minutes) before launch. Classifier now includes a `destructive` threat category (#1721). `pathExtensions` config (#1841) prepends validated tool directories to SAFE_ENV.PATH so user-installed tools (bun, node, cargo) are discoverable. `SAFE_ENV.HOME` defaults to `/tmp`; validated home override via `buildSafeEnv({ home })`. PATH entries validated: absolute, no colons, per-dir uid ownership. HOME-dependent shims excluded in sandboxed mode. Regression tests added (#1914): abort resolves for long-running commands, child process-group kill (PID-verified), fd-redirecting descendant kill, pre-abort guard (no spawn when signal already aborted), SIGKILL escalation for SIGTERM-immune processes. `workspaceRoot` MUST point at an existing real directory — the underlying `@koi/bash-security` path-validator fail-closes when the base is missing so an attacker cannot race a symlink between validation and spawn (PR #2040) | `bash-exec`, `bash-background`, `bash-track-cwd`, `bash-ast-too-complex` |
| `@koi/tools-builtin` | Built-in tools: Glob, Grep, ToolSearch, Read, FsRead. Interaction tools: TodoWrite, EnterPlanMode, ExitPlanMode, AskUserQuestion. Credential path guard (`createCredentialPathGuard`) blocks fs tool access to `~/.ssh`, `~/.aws`, etc. — default-on via `RuntimeConfig.credentialPathGuard` | `glob-use`, `todo-write`, `plan-mode`, `ask-user`, `interaction-full` |
| `@koi/sandbox-os` | OS-level sandbox executor (macOS Seatbelt / Linux bwrap). Sandbox adapter now injected into `createBashTool()` at L3 (DI pattern replaces standalone `run_sandboxed` tool). Seatbelt deny rules fixed: explicit `file-read-data`/`file-read-metadata` instead of invalid `file-read*` wildcard. Multi-backend executor (issue #1641): adapter declares capabilities `{exec, network, filesystem-rw}` priority `0` so `@koi/sandbox-router` can match it during capability-driven selection (`spawn`/`copy-files` intentionally NOT declared — instance.spawn unimplemented and readFile/writeFile throw) | `sandbox-exec` |
| `@koi/sandbox-docker` | Docker-backed `SandboxAdapter` — translates `SandboxProfile` (network/resources/env) into Docker container options via injectable `DockerClient` (default wraps `docker` CLI through `Bun.spawn`). `createDockerInstance` exposes `exec`/`readFile`/`writeFile`/`destroy` over a container; binary-safe IO via base64 round-trip; shell-quoted paths. `detectDocker()` probes daemon availability so callers can fail closed when Docker is missing. Multi-backend executor (issue #1641): declares `{exec, copy-files, network, filesystem-rw}` priority `10`. **Cross-session persistence (#1375 follow-up to #2082):** when the underlying client provides the `findContainers`/`inspectContainer`/`startContainer` triple, the adapter additionally declares `persistence` and exposes `findOrCreate(scope, profile)` + `destroyScope(scope)`; persistent instances expose `instance.detach()` (stop without remove). Trust model uses a private `ScopeRegistry` (default: per-`(scope,id)` filename layout under `${KOI_SANDBOX_DOCKER_STATE_DIR}` or `${XDG_STATE_HOME}/koi-sandbox-docker/scopes/`) so label-only daemon peers cannot hijack a scope. Cross-process safety: deterministic `--name` + name-conflict retry, atomic `forgetIfMatches(scope, expectedId)` CAS-by-id (no peer's freshly-recorded entry can be erased by a stale cleanup), preflight on `destroyScope` refuses all mutations when foreign siblings exist, transient `docker ps`/`inspect`/`image inspect` failures throw rather than silently losing ownership. Profile fingerprint covers `(image, resolveImageId(image), profile)` so a mutable tag repointed at new content invalidates reuse. End-to-end validated against real Docker via `e2e/docker-conformance.e2e.ts` + `e2e/race-runner.ts` (findOrCreate, destroy-vs-create, squatter, profile-drift scenarios) | standalone |
| `@koi/sandbox-ssh` | SSH-backed `SandboxAdapter` (issue #1641) for remote command execution via `ssh2`. Key auth only (no password — by design). POSIX shell quoting (`composeCommandLine` / `quoteArg`); per-scope connection pool with idempotent destroy + 200 ms close-event timeout; `instance.exec` honors `timeoutMs`/`signal`/`onStdout`/`onStderr` and surfaces transport drops as a typed reject (no silent `exitCode=0` leaks). Declares `{exec, copy-files, persistence, network}` priority `20`; provides `instance.detach()` so persistence is honest. `SandboxSshTarget` carries optional `port` (default 22). | standalone |
| `@koi/sandbox-router` | Multi-backend executor router (issue #1641) — `createSandboxRouter({ adapters, degradedThreshold? })` matches the requested `SandboxProfile.required` capabilities against each adapter's declared `AdapterCapabilities`, sorts by priority (lower first, ready-before-degraded), and falls back through the chain on `create()` failure. Per-adapter init promises prevent one hung backend from starving creates against ready siblings. Idempotent `shutdown()` marks records terminated FIRST then awaits `adapter.shutdown()`; concurrent `create()` re-checks state across the await boundary so an instance produced after shutdown is destroyed and `UNAVAILABLE/router-shutting-down` is surfaced to the caller. Returns `SelectionDecision` with full attempt audit trail | standalone |
| `@koi/sandbox-conformance` | Reusable conformance test suite (issue #1641) for any `SandboxAdapter` implementor. Exposes `describeLifecycleConformance`, `describeCreateDestroyConformance`, `describeCapabilityHonestyConformance`, and the umbrella `describeSandboxConformance`. Capability-honesty group catches lying adapters (`persistence` declared without `findOrCreate.detach`, `spawn` declared without `instance.spawn`, `copy-files` declared without `readFile`/`writeFile`). Per-adapter conformance suites run inside sandbox-os, sandbox-docker, sandbox-ssh `__tests__/conformance.test.ts` | standalone |
| `@koi/sandbox-executor` | Subprocess-backed `SandboxExecutor` for isolated code execution — spawns a Bun subprocess that dynamic-imports user code and frames `{ ok, output \| error }` on stderr after `__KOI_RESULT__`. Temp dir cleaned up in `try/finally`; runner calls `process.exit(0)` after success so user-code event-loop anchors don't cause TIMEOUT misclassification; explicit `stdin: "ignore"` closes the inherited-stdin leak. Maps kill (`exit 137 / -9`) to `TIMEOUT` (when duration ≥ deadline) or `OOM`; missing marker → `CRASH`. Output cap (`maxOutputBytes`) only trims the stack snippet, never the marker search (issue #1375) | standalone |
| `@koi/tools-web` | Web fetch and search tools. SSRF defence routed through `@koi/url-safety` (L0u): `web-fetch-tool` uses a DNS-free `preflightBlockReason`; `web-executor` wraps `createSafeFetcher` for strict-authoritative DNS + per-hop redirect revalidation + HTTP IP-pinning. Fetch and search abort timers are clamped to the package `MAX_TIMEOUT_MS` (60 s). `web_fetch` results expose a `cached: boolean` field so callers distinguish LRU cache hits from fresh network fetches without inferring from latency. Cache admission honors origin directives (`no-store`/`no-cache`/`private`/`must-revalidate`/`max-age=0`/`Pragma: no-cache`/past `Expires`/`Vary`), stores only status 200, skips GET/HEAD-with-body, and caps each entry to `min(cacheTtlMs, max-age − Age)` per RFC 7234 (private end-cache — `s-maxage` ignored). `web_fetch` accepts a per-call `noCache: true` that evicts the prior entry and refuses any stale fallback (no restore on failure; fenced write-back so a slower noCache response can't clobber a newer concurrent write). The CLI opts in with a 60 s default TTL via `KOI_WEB_CACHE_TTL_MS`; malformed values fail startup loudly (#1903) | `web-fetch`, `url-safety-block`, `url-safety-dns-block` |
| `@koi/tool-notebook` | Jupyter notebook manipulation: read, add, replace, delete cells. Workspace-contained path validation with symlink-safe realpath checks | `notebook-read`, `notebook-add-cell` |
| `@koi/lsp` | LSP client integration: hover, goto-definition, find-references, document/workspace symbols. Mock client for golden queries. Tool names use `lsp__server__op` format for OpenAI API compatibility | `lsp-hover` |
| `@koi/tool-browser` | Browser automation tools (16 ops) via `BrowserDriver` interface. Mock driver for golden queries. Post-redirect URL policy enforcement. `validateUrlScheme` blocks dangerous schemes | `browser-snapshot` |
| `@koi/skill-scanner` | AST-based security scanner for SKILL.md code blocks. Detects dangerous APIs (eval, child_process, process.binding, vm), bracket-notation bypasses (`obj["method"]()`), template-literal keys (`` obj[`method`]() ``), obfuscation, prompt injection, exfiltration, and SSRF patterns. Used by `@koi/skills-runtime` as a fail-closed gate (#1572) | standalone |
| `@koi/skills-runtime` | Multi-source skill loader (`mcp < bundled < user < project` precedence) with fail-closed AST security gate (`@koi/skill-scanner`). Progressive loading: `discover()` reads frontmatter AND runs `@koi/skill-scanner` on each skill's SKILL.md body before inserting into the registry — skills with findings at or above `blockOnSeverity` are excluded from `discover()`/`query()`/`describeCapabilities()` so malicious skills never reach the model (#1722). Blocked skills remain visible via `loadAll()` as `PERMISSION` errors for operator observability. `load()` promotes to `SkillDefinition` with body + scan (fail-safe for direct loader contexts). In-memory registry: `query(filter?)` filters by tags/source/capability (AND semantics for multi-tag). `invalidate(name?)` for cache control — targeted invalidation of blocked skills triggers a re-read + re-scan with tier-precedence resolution and per-name generation guards for concurrent safety. Inflight deduplication for concurrent access. `registerExternal(skills)` injects non-filesystem skills (e.g., MCP-derived) with separate cache lifecycle. Blocked filesystem names shadow same-named external skills to prevent adversarial name squatting. Execution modes: `execution: inline` (default, context injection) or `execution: fork` (sub-agent spawn via `mapSkillToSpawnRequest()`). `createSkillProvider()` loads all skills at attach time and inserts each as a `SkillComponent` under `skillToken(name)` in the agent ECS. `createSkillInjectorMiddleware()` (resolve phase, priority 300) reads skill components and prepends their content into `request.systemPrompt`, sorted alphabetically for deterministic output. Empty-body skills (MCP metadata-only) filtered from content injection but kept for capability discovery. Accepts `Agent \| (() => Agent)` for lazy resolution. Progressive disclosure (#1642): three-tier load model — Tier 0 metadata from `discover()`/`query()`, Tier 1 full body from `load()` with bounded LRU cache (`cacheMaxBodies`, default unbounded), Tier 2 on-demand reference files from `loadReference(name, refPath)` gated by a frontmatter `references:` allowlist. Tier 2 enforces a layered security boundary: path hygiene (empty / null-byte / absolute / `..` / backslash rejected), extension allowlist (Markdown + JS/TS sources only — shell/python/config formats refused until the scanner has whole-file coverage for them), skills-root containment, parent-dir realpath + inode pin against ancestor symlink swaps, `O_NOFOLLOW` + `fstat.nlink===1` on the final open, bounded read via `handle.read()` against a `maxBytes + 1` cap so in-place growth between stat and read still trips the ceiling, binary-byte sniff, and the same `scanSkill()`/`scan()` dispatch Tier 1 uses. Tier 2 authorization is the intersection of the discovery-time snapshot (frozen until rediscovery — prevents unreviewed allowlist expansion) and the current on-disk list (re-read per call so revocations take effect immediately), and a runtime + per-skill epoch guard drops stale cache writes and in-flight reads after `invalidate()` / `invalidate(name)` / `registerExternal()`. Tier 2 also routes through `load(name)` so a body that starts tripping the scanner post-discovery revokes its declared references. New config callbacks: `onMetadataInjected(count)` (fired once per unique Tier 0 snapshot), `onSkillLoaded({ name, source, bodyBytes, cacheHit })`, `onSkillEvicted({ name, reason: "lru" | "invalidate" | "external-refresh" })` | `skill-load` |
| `@koi/skill-tool` | SkillTool meta-tool — model invokes `Skill(name, args?)` for on-demand skill loading. Budget-aware advertising (3-phase: full → truncated → names-only). Inline mode returns substituted body; fork mode delegates to `SpawnFn` with recursion guard. Structural `SkillResolver` interface avoids cross-L2 imports. Fail-closed spawn validation: empty allowlists, reserved-only tools, missing spawnFn all return typed errors (#1594) | `skill-tool-use` |
| `@koi/harness` | CLI harness: single-prompt + interactive REPL wiring, TUI adapter bridge, fail-closed stream guards. Renders `plan_update` and `task_progress` events in verbose mode (#1555). `tool_result` events (#1583) are silent in raw-stdout rendering — `tool_call_end` carries the verbose summary; the TUI bridge consumes `tool_result` separately for tool block updates | standalone |
| `@koi/loop` | Convergence loop primitive — `runUntilPass()` re-runs an agent turn against a verifier gate (`createArgvGate`/`createFileGate`/`createCompositeGate`) until it passes, the iteration budget exhausts, a consecutive-failure breaker trips, or the caller aborts. Pure state machine with 5 terminal states (`converged`/`exhausted`/`aborted`/`circuit_broken`/`errored`). Argv-only subprocess gates (no shell strings), minimal default env allowlist with opt-in inheritance, credential redaction in rebuild-prompt, strict `stopReason === "completed"` gate so truncated turns never reach the verifier, and iterator cleanup fence (100 ms budget) promoting orphaned streams to `errored`. Wired into `koi start --until-pass` and `koi tui --until-pass` | `loop-until-pass` |
| `@koi/code-executor` | `execute_script` tool — runs JS/TS through an injected `SandboxExecutor` (typically `@koi/sandbox-executor`). Bun.Transpiler for TS, async-function wrapper so user `return`/`await` work, sandbox failures (`TIMEOUT`/`OOM`/`CRASH`/`PERMISSION`) pass through with their typed code; transpile failures map to `TRANSPILE`. No `callTool` from inside scripts — single round-trip semantics. Provider attaches under `toolToken("execute_script")` at `COMPONENT_PRIORITY.BUNDLED`. Issue #1379 | standalone |
| `@koi/tool-exec` | `execute_code` tool: runs model-authored TypeScript/JavaScript in an isolated Bun Worker via registered Koi tools, returning only the final script result. Trust gate (`ACKNOWLEDGE_UNSANDBOXED_EXECUTION`) required on both `createExecuteCodeTool` and `executeScript`; returns `PERMISSION` error without it. Per-call `AbortController`, single `settle()` finalizer, `DOMException("TimeoutError")` for timeout abort, `inFlightAtSettlement` flag for indeterminate cancellation states, host-authoritative concurrency and tool-call budget violation detection. Budget exhaustion fails the whole script even if worker code catches the rejected tool call. | `tool-exec-code-use` |

> **L0u packages also wired:** `@koi/tools-core` (`buildTool()` factory), `@koi/validation`, `@koi/task-board`, `@koi/rules-loader` (hierarchical project rules file injection — discovers CLAUDE.md/AGENTS.md/.koi/context.md from cwd to git root, merges root-first into system prompt), `@koi/context-manager` (`enforceBudget()` — token-aware transcript compaction with micro/full cascade; `resolveConfig()` + `budgetConfigFromResolved()` for per-model window resolution; golden tests in `golden-queries.test.ts` cover micro-compaction and model-registry window lookup, #1623), `@koi/replay` (VCR cassette replay — `loadCassette`, `createReplayAdapter`, `createReplayContext`, `createCassetteRecorder`, `createRegistry`; re-exported from `@koi/runtime` for downstream test suites; cassette format `cassette-v1` with volatile field stripping, #1629) are L0u (utility) packages depended on by `@koi/runtime` but not subject to the L2 doc/golden-query gates — their docs live under `docs/L0u/` or `docs/L2/`.

### Phase-2 runtime-level L2 wiring (#1778)

Four L2 packages are now composed directly into `createRuntime()` via opt-in config fields (default behavior is unchanged when the config is absent):

| Config field | Package(s) | Exposed as |
|---|---|---|
| `config.audit: { sink: { kind: "ndjson" \| "sqlite", ... } \| AuditSink, signing?, ... }` | `@koi/middleware-audit` + `@koi/audit-sink-ndjson` \| `@koi/audit-sink-sqlite` | Middleware installed in the observe phase; runtime-owned sinks are flushed and closed on `dispose()`. Discriminated via a nominal `isAuditSink()` check (`typeof v.log === "function"`) so a caller sink carrying `kind` as metadata is not misclassified as a config. |
| `config.browser: { backend: BrowserDriver, operations?, isUrlAllowed?, ... }` | `@koi/tool-browser` | `handle.browserProvider` (ComponentProvider). The `backend` must be unshared per runtime — the inner provider disposes it on its detach ref-count → 0 transition, and sharing across runtimes would steal tabs/cookies on first detach. |
| `config.lsp: LspProviderConfig` | `@koi/lsp` | `handle.lspProvider: () => Promise<{ provider, clients, failures }>` (lazy thunk). Startup is deferred until the caller invokes the thunk — a runtime disposed without ever asking for LSP spawns no subprocesses. `dispose()` releases pooled clients (fail-closed fallback to `close()` on pool error) or closes them directly, mirroring a runtime-side attach/detach counter so a completed createKoi lifecycle is not re-cleaned. `LspProviderConfig.servers` is optional (#2005) — `{ autoDetect: true }` is now a valid config with no `servers` field required. |
| `config.memoryFs: MemoryStoreConfig` | `@koi/memory-fs` | `handle.memoryStore: MemoryStore`. A `MemoryToolBackend` adapter for `@koi/memory-tools` is deferred to a follow-up. |

Dispose hardening:
- Per-resource latches (`auditCleanedUp` / `lspCleanedUp`) make repeated `dispose()` calls idempotent so the existing retryable-shutdown pattern still works when an unrelated leg (channel/adapter) fails.
- `BuiltAudit.close()` runs `mw.flush()` + sink `close()` in a best-effort aggregate — a failing flush cannot leak sink resources.
- Constructor-time throws (e.g. `createMemoryStore` rejecting an invalid `memoryFs`) close any runtime-owned audit sink and dispose caller-supplied browser backends via fire-and-forget before rethrowing.
- LSP attach/detach wrappers advance counters only after the inner call resolves so a single-agent attach rejection (`@koi/tool-browser` / `@koi/lsp` throw when a second agent attaches) does not falsely mark the provider as attached.

All four L2 packages already had pre-existing golden query coverage (`Golden: @koi/memory-fs`, `Golden: @koi/tool-browser`, `Golden: @koi/lsp`, `audit-log` trajectory for middleware-audit + sinks) before this wiring; no new fixtures were recorded.

### Spawn Inheritance Coverage (#1425)

The spawn path has three golden trajectories proving narrowing at the `ModelRequest.tools` boundary:

| Trajectory | What it proves |
|-----------|----------------|
| `spawn-inheritance` | Runtime `toolDenylist=["Glob"]` — Glob absent from child model call |
| `spawn-allowlist` | Runtime `toolAllowlist=["Grep"]` — child sees only Grep |
| `spawn-manifest-ceiling` | `manifest.spawn.tools.policy=allowlist` — engine enforces ceiling without any per-call list |

### Fork Mode + Coordinator Allowlist Coverage (#1241)

Two standalone golden queries (no LLM required) prove the coordinator tool surface:

| Query | What it proves |
|-------|----------------|
| `COORDINATOR_TOOL_ALLOWLIST` shape | `agent_spawn` present; `Glob`/`Grep`/`ToolSearch` absent; allowlist matches manifest spawn config (single source of truth) |
| Coordinator manifest spawn policy | `spawn.tools.policy === "allowlist"`; manifest list equals `COORDINATOR_TOOL_ALLOWLIST` |

The `spawn-fork` and `spawn-coordinator` trajectory fixtures record fork mode spawning and coordinator-ceiling enforcement end-to-end.

---

## Adding a New L2 Package

Follow the Doc → Tests → Code workflow:

1. **Doc first**: create or update `docs/L2/<name>.md`
2. **Update this file**: add a row to the table above
3. **Wire**: add dep to `packages/meta/runtime/package.json`
4. **Golden query**: add `QueryConfig` to `scripts/record-cassettes.ts`
5. **Record**: `OPENROUTER_API_KEY=... bun scripts/record-cassettes.ts`
6. **Assert**: add `describe("Golden: @koi/<name>", ...)` to `golden-replay.test.ts`
7. **CI gates**: `check:orphans`, `check:golden-queries`, `check:doc-wiring` must all pass

## ATIF Decision Metadata (reportDecision)

Every decision-making L2 middleware must emit structured decision metadata via
`ctx.reportDecision?.(decision)` when something interesting happens (action
taken, not passthrough). The trace wrapper injects a per-call `reportDecision`
callback that buffers decisions into the MW span's `metadata.decisions[]` array.

**Rules:**
- Only emit when an actual decision was made (block, deny, inject, rewrite,
  detect, filter). Passthrough turns emit no decision — silence is the signal.
- Include context: what was checked (`toolInput`, `toolId`, `event`), what
  action was taken (`action`, `aggregated`, `phase`), and **why** (`reason`,
  `source`, per-hook reasons).
- Truncate large payloads with a 300-char preview helper — never emit raw
  tool inputs/outputs that could leak secrets.
- The trajectory store is the source of truth; TUI's `/trajectory` command
  reads from it — no need to wire decisions into mid-turn EngineEvents.

The CI enforcement describe block in `golden-replay.test.ts` runs all
decision-making middleware together and asserts every one emits at least one
span with a non-empty `decisions` array. Add new L2 middleware to that block
when introducing new decision-making packages.

Current middleware with decision metadata:
- `@koi/middleware-permissions` — filter phase (when filtering) + every tool execute
- `@koi/hooks` — when hooks match tool.before / tool.succeeded / compact.before
- `@koi/skills-runtime` — only when skills are actually attached
- `@koi/middleware-exfiltration-guard` — on every secret detection / fail-closed path
- `@koi/middleware-semantic-retry` — on retry/abort actions (silent on success)
- `@koi/model-router` — on every model call: selected target, attempted targets, fallback_occurred, latency_ms

### Trajectory Visibility

The runtime now exposes `createDecisionLedger()` on `TuiRuntimeHandle`, wrapping the trajectory store to resolve the TUI's fixed document ID. All integrated L2 middleware report decisions via `ctx.reportDecision()` for TUI trajectory visibility.

---

## CI Gates

| Gate | What it checks |
|------|---------------|
| `check:orphans` | Every L2 dep of `@koi/runtime` appears in `check:layers` graph |
| `check:golden-queries` | Every L2 dep has golden query assertions |
| `check:doc-gate` | Every L2 package has a `docs/L2/<name>.md` |
| `check:doc-wiring` | Modified L2 packages and changed L3 wiring have updated docs |

> **Maintenance note (PR #1506):** Lint-only fixes applied to integrated packages (@koi/event-trace, @koi/model-openai-compat, @koi/mcp, @koi/middleware-permissions). No wiring changes; L2 package set is unchanged.

> **Wiring (PR #1511):** `@koi/sandbox-os` added. Golden query `sandbox-exec` exercises the path-locked `run_sandboxed` tool under macOS Seatbelt / Linux bwrap. The golden query is trajectory-only (no cassette replay) — the sandbox executes live `ls` during recording; CI validates the fixture fields.

> **Wiring (issue #1375):** `@koi/sandbox-docker` and `@koi/sandbox-executor` added as standalone L2 packages. Two standalone golden queries per package (4 total, no LLM): docker adapter create + UNAVAILABLE error path; subprocess-executor identity passthrough + TIMEOUT classification. Both packages depend only on `@koi/core` and Node built-ins; `findOrCreate` scope persistence deferred.

> **`@koi/sandbox-os` Linux backend hardening (PR #1617, issue #1339):** No `@koi/runtime` wiring changes. Internal improvements to the integrated `@koi/sandbox-os` L2 package: AppArmor usability probe (`bwrap --unshare-all` smoke-test, same namespace flags as production), per-exec named systemd transient scopes for cgroup lifecycle management, `denyRead` file vs. directory handling (`--bind /dev/null` for files, `--tmpfs` for dirs), and `/bin/bash` absolute path in the ulimit wrapper. These changes affect runtime behavior on Linux only.

> **Auth wiring (PR #1438):** `@koi/fs-nexus` gained inline OAuth support for connectors requiring browser auth (gdrive, gmail, etc.). New runtime helpers: `resolveFileSystemAsync()` returns `{ backend, operations, transport }` — callers must pass `filesystemOperations` to `createRuntime()` to preserve write/edit grants and use `transport.submitAuthCode()` to wire the remote paste-redirect flow. `RuntimeConfig.filesystem` now also accepts a pre-created `FileSystemBackend`. New golden trajectory `gdrive-oauth-e2e` captures the full auth flow: mount → auth_required → localhost callback → token exchange. Requires nexus-fs `>= 0.4.6`.

> **Wiring (PR #1519):** `@koi/bash-security` (L0u) and `@koi/tools-bash` (L2) added. `createBashTool()` provides a cwd-contained, env-isolated bash execution tool with a classifier pipeline (allowlist → injection → path → command → exfiltration), process-group kill on abort/timeout, and a spawn-error handler. Full filesystem confinement requires injecting an OS sandbox via `wrapCommand` at the L3 layer. Golden query `bash-exec` records a trajectory of the Bash tool being called and returning stdout.

> **Retry tracing (PR #1502):** `RuntimeConfig.retrySignalReader` threads a `RetrySignalReader` (L0) into per-stream event-trace middleware, so retry annotations (`retryOfTurn`, `retryAttempt`, `retryReason`) appear in production ATIF trajectories. Callers create a `RetrySignalBroker` externally and pass the writer to semantic-retry, reader to runtime. `agentName` is now also threaded (previously hardcoded as `"runtime"`). `@koi/middleware-semantic-retry` gains hook-blocked tool response detection (`blockedByHook` metadata) — treated as a no-op on retry state (event-trace handles classification independently). `@koi/context-manager` surfaces `preservationFailed`/`preservationError` on `BudgetEnforcementResult` when `onBeforeDrop` callbacks fail (fail-open).

> **Wiring (PR #1518):** `@koi/agent-runtime` wired via `config.agentDirs` shortcut. `RuntimeConfig` now accepts `agentDirs?: AgentResolverDirs` — `createRuntime()` calls `createAgentResolver(agentDirs)` internally when no explicit `resolver` is provided. Load warnings and conflicts emitted via `console.warn` and exposed on `RuntimeHandle.agentWarnings`/`agentConflicts`. Golden queries are 2 standalone assertions (no LLM) in `describe("Golden: @koi/agent-runtime")`; end-to-end spawn coverage is provided by the existing `spawn-agent` trajectory.

> **Monotonic timestamps (PR #1569, issue #1558):** `RuntimeConfig.clock` now accepts a base clock function; `createRuntime()` wraps it in a per-stream `createMonotonicClock()` from `@koi/event-trace` so concurrent sessions never interfere. Clock injection threaded to all trajectory emitters: event-trace, trace-wrapper, hook-observer, mcp-lifecycle, and harness step helpers. The ATIF store enforces monotonicity at append time as a safety net for L1 emitters. Added `validate-timestamps.ts` script for fixture hygiene. All 28 golden fixtures re-recorded with strictly increasing timestamps. Resolves #1558 and #1559.

> **Prompt cache stability (PR #1554, issue #1554):** `@koi/model-openai-compat` now sorts tools alphabetically before sending to the model API for KV cache prefix stability. Capability banner is cached per answer attempt and reused on stop-gate retries (replaces `skipCapabilityInjection`). `promptPrefixFingerprint` (SHA-256 of system prompt + sorted tool payload) added to `ModelResponse.metadata` for cache drift diagnostics. `@koi/context-manager`'s `micro-compact.ts` documents cache-boundary constraints.

> **Trace wrapper MW span refinements (current branch):** `wrapMiddlewareWithTrace` now records the middleware's decision (`"allowed"` / `"blocked"`) as the `response.text` for `wrapModelCall` and `wrapToolCall` spans, based on whether `next()` was called. `wrapModelCall` success spans omit `request`/`response` (those are on the model step itself); only error spans include the request preview. `wrapModelStream` spans also omit request/response — `wrapStreamForTrace` accumulates `text_delta` chunks (capped at 500 chars) but the accumulated text is passed to the `onComplete` callback only (not stored on the span). This keeps MW spans focused on middleware behavior (duration, outcome, decision, deltas) rather than duplicating the model step's content.

> **Wiring (PR #1560):** `@koi/skills-runtime` (L2) and `@koi/skill-scanner` (L0u) added. `createSkillProvider()` is a `ComponentProvider` that calls `runtime.loadAll()` at agent attach time, inserting each loaded skill as a `SkillComponent` under `skillToken(name)`. Golden query `skill-load` proves the full wiring path (MCP connect → skill load → model call). Security hardening applied: include boundary tightened to skill's own directory, Windows-safe path checks via `path.relative()`, `node:`-prefix normalization for dangerous-module detection, static `ImportDeclaration` detection, and CommonMark tilde/indented-fence support. Follow-up gaps filed as #1571 (fence length enforcement) and #1572 (bracket-notation bypass). PR #1587 closes #1572: bracket-notation (`obj["method"]()`) and template-literal (`` obj[`method`]() ``) member calls now resolved as static strings via `getComputedStringProperty()` + `flattenMemberChain()` in `walker.ts`. `@koi/skill-scanner` wired as direct `@koi/runtime` dependency with 2 standalone golden queries.

> **Skill injection (PR #1590):** `createSkillInjectorMiddleware()` added to `@koi/skills-runtime`. Reads `SkillComponent` entries from the agent ECS at each model call and prepends their content into `request.systemPrompt`. Skills sorted alphabetically for deterministic prompt text and prompt-cache stability. Accepts `Agent | (() => Agent)` for lazy resolution (middleware created before `createKoi` assembles the entity). Golden replay test captures `ModelRequest` in the adapter terminal and asserts `systemPrompt` contains skill content. `koi tui` wired: loads skills from `~/.claude/skills/` at startup via `createSkillsRuntime().loadAll()` and prepends to the system prompt.

> **Interaction tools wiring:** `createInteractionProvider` exported from `@koi/runtime` wraps all four interaction tools (TodoWrite, EnterPlanMode, ExitPlanMode, AskUserQuestion) into an ECS `ComponentProvider`. State (todo items, plan mode flag, plan content) is ephemeral — stored in closure locals per `attach()` call, lost on process restart. `onEnterPlanMode` / `onExitPlanMode` hook callbacks let the harness enforce the read-only permission gate and restore pre-plan permissions + `allowedPrompts` after approval. `AskUserQuestion` is omitted entirely when `elicit` is not provided. Swarm-path wiring (`isTeammate`, `writeToMailbox`) belongs to `@koi/swarm` (#1416) — `createInteractionProvider` defaults to the non-swarm path. Golden queries: `todo-write`, `plan-mode`, `ask-user`, `interaction-full`.

> **Security defaults (PR #1503):** `@koi/middleware-exfiltration-guard` now default-on for adapters with terminals. `createRuntime()` auto-installs the guard with `action: "block"`. New `RuntimeConfig.exfiltrationGuard` (`false` to disable, `Partial<ExfiltrationGuardConfig>` to customize). Explicit config on terminal-less adapters throws (fail-closed). The middleware gained `wrapModelCall` (non-streaming scan of `content` + `richContent`), tool output scanning (always-on, dual JSON+String representation), and stream chunk buffering (`text_delta` + `thinking_delta` + `tool_call_start/delta/end`). `ExfiltrationEvent.location` extended with `"tool-output"`. `@koi/tools-builtin`'s `createCredentialPathGuard()` now threaded through both `createFileSystemTools()` (dispatch) and `createFileSystemProvider()` (ECS provider via bound factory closures). New `RuntimeConfig.credentialPathGuard` (`false` to disable). `wrapModelStream` overflow state machine fixed: redact mode uses `textOnlyBuffer` (no hidden content leakage), block mode fails closed, warn mode replays held chunks preserving tool_call structure.

> **Nexus trajectory persistence (PR #1592, issue #1469 Phase 1):** `@koi/fs-nexus` now exports `createHttpTransport` for transport reuse by non-filesystem consumers. `@koi/runtime` gains `createNexusAtifDelegate` — a Nexus-backed `AtifDocumentDelegate` so ATIF trajectory documents persist to a Nexus server and survive process restarts. New `RuntimeConfig.trajectoryNexus` option (mutually exclusive with `trajectoryDir`). Nexus transport lifecycle threaded into `runtime.dispose()`. Shared path-encoding utils extracted from `fs-delegate.ts`. Design: only `NOT_FOUND` maps to undefined (auth/permission errors propagate), `glob()` over `list()` to avoid truncation, basePath validated at construction, single-writer-per-docId mutex (OCC deferred to Phase 2).

> **Outcome linkage (PR #1607, issue #1465 Phase 1):** Decision-outcome correlation infrastructure. L0 adds `DecisionCorrelationId` branded type, `OutcomeReport`, `OutcomeStore` (put+get). `@koi/event-trace` allowlists `decisionCorrelationId` — sourced only from `request.metadata` (tools cannot forge), validated as bounded non-empty non-whitespace string (max 256 chars), preserved on failure/hook-blocked paths, invalid IDs surface `decisionCorrelationIdPruned` diagnostic. `RuntimeHandle.outcomeStore` populated when `trajectoryNexus` is configured — Nexus-backed store scoped under `{trajectoryBasePath}/outcomes/`. In-memory store available for tests/dev. Phase 2: `report_outcome` tool, ATIF doc-level aggregation, cross-session search, OCC writes.

> **Bash AST classifier (PR #1660, issue #1634):** `@koi/bash-ast` wired as a new L0u dependency alongside `@koi/bash-security` (retained as prefilter + transitional regex fallback). `@koi/tools-bash` swapped its `classifyBashCommand` import from `@koi/bash-security` to `@koi/bash-ast` and now awaits `initializeBashAst()` at the top of every `execute()` call. Two golden queries cover both classify entry points: `bash-ast-too-complex` records the sync fallback path (no elicit wired — `koi start` / standalone tools), and `bash-ast-elicit` records the async path with an auto-approving elicit callback (closes #1634's fail-closed loop; wired for the TUI via `tui-runtime.ts`). The Codex adversarial review uncovered two P1 bypasses (backslash-escaped words, backslash-newline line continuation) and four P2 bugs (escaped quotes in double-quoted strings, cached rejected init promise, stateful regex flags in matcher, `Tree` WASM memory leak); all six are fixed in the same PR with 18 regression tests in `codex-findings.test.ts`. Shell-escape-related `too-complex` outcomes hard-deny in BOTH paths instead of falling through — the raw-text regex and an interactive user prompt are both fooled by the same escapes the walker rejects. Follow-ups filed: #1661 (variable scope tracking, if prompt noise is intolerable), #1662 (per-command semantic specs for cp/mv/rm/curl/wget/tar/scp/ssh/chmod), #1663 (wrapper-command specs for nohup/timeout/sudo/env/stdbuf/time), #1668 (fuzz harness with escape-aware argv oracle). Persistent approval memory (SQLite-backed allow/ask/deny log across restarts) is orthogonal scope tracked in #1622.

> **Task system hardening (PR #1659, issue #1557 review):** The `@koi/tasks`, `@koi/task-tools`, `@koi/task-board`, and `@koi/spawn-tools` packages ship a 16-issue review punch list. Highlights: `Task.startedAt` field added for accurate `durationMs` on completion; `board.blockedBy(taskId)` method with per-snapshot cache for TUI-polling performance; lazy reverse-adjacency and engine-bridge transition-table collapse in `@koi/task-board`; `@koi/tasks` file-store gains path-traversal defense, single-writer PID lock, bounded I/O concurrency, deque-based output-stream eviction; `TaskRunner` gains a `selfWriteIds` skip-set and mirrored start-path fallback chain; `task_update.execute()` split into per-status handlers (<30 LOC each); `recoverStaleDelegations()` added to `@koi/spawn-tools` for coordinator-restart pending-delegation cleanup; `metadata.delegatedTo` promoted to a **board-enforced pending-only invariant** (stripped at every `in_progress` entry/exit in `@koi/task-board`). Full test-count delta: 282 → 387 tests across the 5 affected packages. Two follow-up issues filed (#1657 `LocalAgentTask`, #1658 `RemoteAgentTask` lifecycles).

> **Persistent approvals (#1622):** `@koi/middleware-permissions` now exports `createApprovalStore` for SQLite-backed cross-session grants. Runtime assemblies can inject an `ApprovalStore` via `createPermissionsMiddleware({ persistentApprovals, persistentAgentId })`. When configured, "always" scope grants survive process restart. Anonymous sessions (no `userId`) cannot create durable grants.

> **OTel opt-in (#1628):** `@koi/middleware-otel` wired as an opt-in L2 dependency. `RuntimeConfig.otel` accepts `true` (defaults), `false`/`undefined` (disabled), or a full `OtelMiddlewareConfig` (`tracerName`, `captureContent`, `meter`, `onSpanError`). When enabled, `createRuntime()` calls `createOtelMiddleware()` and wires `otelHandle.onStep` into event-trace — each step's `otel.traceId`/`otel.spanId` is stamped into ATIF `step.metadata` so both storage backends share the same trace identity. Span hierarchy: `invoke_agent` root → `chat {model}` CLIENT → `execute_tool {tool}` INTERNAL, with retroactive start/end times from step timestamps. Only `@opentelemetry/api` is bundled; host wires SDK.

> **Checkpoint + snapshot-store wiring:** `@koi/checkpoint` and `@koi/snapshot-store-sqlite` wired as new L2 dependencies. `createRuntime()` now threads `resolvePath` from the filesystem backend into the checkpoint middleware config, so checkpoint state files are validated against the workspace sandbox boundary before persistence. The snapshot store uses SQLite/WAL for durable checkpoint storage. Checkpoint middleware is inserted into the middleware chain when configured.

> **`auth_progress` dedup:** `@koi/fs-nexus`'s `createAuthNotificationHandler` now returns a disposable handler that suppresses duplicate `auth_progress` heartbeats per provider. Only the first heartbeat per OAuth flow emits to the channel; later heartbeats are skipped until `auth_required`/`auth_complete` resets the flow. Per-provider epochs + per-send attempt tokens guard stale callbacks across flow boundaries; a 45 s watchdog clears `pending` entries if `channel.send()` never settles; FIFO eviction caps state at 32 providers. No `RuntimeConfig` surface change — hosts that wire the handler (e.g. `koi tui`) must call `handler.dispose()` synchronously before awaiting the filesystem backend's `dispose()` so pre-queued microtasks short-circuit.

> **Decision ledger (PR #1680, issue #1469 Phase 2a):** `@koi/decision-ledger` wired as a new L2 dependency. `RuntimeHandle.createDecisionLedger` factory field added to `RuntimeHandle` — returns a `DecisionLedgerReader` that joins the runtime's live `trajectoryStore` with an optional `AuditSink` (via override) and `ReportStore` (from `config.reportStore` or override). `undefined` when no `trajectoryStore` is configured. The ledger is a read-only diagnostic projection: it never writes, never mutates upstream state. Integrity filtering applies only at the top-level `getBySession()` boundary — nested `childReports` with different sessionIds are preserved because delegated sub-agent runs legitimately use `sessionId(\`delivery-${childId}\`)` per `kernel/engine/delivery-policy.ts`. 50+ adversarial-review rounds surfaced 9 correctness fixes: session-integrity filtering, dedicated state discriminants, deterministic audit sort on `(timestamp, turnIndex, originalIndex)`, trajectory `stepIndex` sort, raw-payload soft-ceiling (not filtered lane size), iterative cycle-safe tree walker, trajectory trust model fields, catastrophic-vs-degraded error split, and `integrityLeakCounts` propagation across both partial-leak and full-violation states. Phase 2(a) ships trajectory-only surfacing in practice because no default v2 `AuditSink` or `ReportStore` implementation exists yet — both tracked as follow-up issues. Standalone goldens only (3 tests in `golden-replay.test.ts`): recorder doesn't wire `AuditSink` or `ReportStore`, denial tracker clears on `onSessionEnd`, report finalizes on `onSessionEnd`, so a replay-driven four-sink assertion isn't achievable without lifecycle extensions (also filed as follow-up).

> **Model router (PR #1688, issue #1626 Phase 2):** `@koi/model-router` wired as a new L2 dependency. `createModelRouterMiddleware()` at priority 900 (outermost) intercepts `wrapModelCall` and `wrapModelStream`, routing through a configurable fallback chain with per-target circuit breakers, retry backoff, latency tracking, and an optional async health probe. `RouteDecision` (selectedTargetId, attemptedTargetIds, fallbackOccurred) exposed from both `route()` and `routeStream()`; middleware emits these as `router.*` keys via `ctx.reportDecision` so they appear as `decisions[]` on the `MW:model-router` ATIF span. Golden trajectory `model-router` recorded; two `golden-replay.test.ts` assertions verify the span shape.
>
> **Model router CB window fix (PR #2020, issue #2017):** `MODEL_ROUTER_CB_DEFAULTS.failureWindowMs` raised from 60s to 300s in `@koi/model-router` config resolution. The shared `DEFAULT_CIRCUIT_BREAKER_CONFIG` in `@koi/errors` is unchanged. With `failureThreshold: 5` and ~13–15s/turn LLM latency, 5 consecutive primary failures span ~65–75s — exceeding the old 60s window so failures expired before threshold was reached. 300s accommodates up to ~60s/turn × 5 failures without affecting other `@koi/errors` circuit breaker consumers.

> **Model router RuntimeConfig opt-in (issue #1704):** `RuntimeConfig.modelRouterMiddleware?: KoiMiddleware` added. Pass a pre-constructed `createModelRouterMiddleware(...)` instance and `createRuntime()` appends it as the innermost model-call interceptor (after exfiltration guard). Skipped if a middleware named `"model-router"` is already present in `config.middleware`. `@koi-agent/cli` wires this via `KOI_FALLBACK_MODEL` env var (comma-separated model names): the TUI builds a per-model `ProviderAdapter` map (all sharing the same API key/base URL) and passes `modelRouterMiddleware` to `createTuiRuntime()`. Startup log: `[koi/tui] model-router: <primary> → [<fallback>, ...]`.

> **Outcome evaluator (PR #1706, issue #1686):** `@koi/outcome-evaluator` wired as a new L2 dependency. `createOutcomeEvaluatorMiddleware()` evaluates agent output against a structured `OutcomeRubric` using a separate grader model call after each turn's `onTurnEnd`. Failing criteria are injected as structured feedback into the next turn's context block until all required criteria pass or `maxIterations` is exhausted. Circuit breaker (`maxConsecutiveIdenticalFailures`) prevents infinite loops when the agent produces the same failing output repeatedly. Optional `isolateCriteria` mode fires one grader call per criterion to prevent halo effects. Standalone goldens only (2 tests in `golden-replay.test.ts`): event ordering (`evaluation.start` fires before `evaluation.end`) and pass-through behavior when all criteria pass on the first attempt.

> **Event-trace turnIndex metadata (PR #1758):** `@koi/event-trace`'s `buildModelStep` now threads `ctx.turnIndex` into `metadata.turnIndex` on agent model call steps. This is a data-only change in the L2 package — no `@koi/runtime` wiring changes needed. The field enables downstream consumers (TUI `/trajectory` view, ATIF analysis) to identify sub-turn boundaries within a single engine `run()` call.

> **`@koi/middleware-audit` DTS build fix (#1742):** `@koi/middleware-audit`'s Ed25519 signing path moved from `Buffer.from(...)` to `TextEncoder` + `Uint8Array(...)`. No `@koi/runtime` wiring change — the middleware is still composed via the existing slot — but the DTS build of `@koi/runtime` (which transitively depends on `@koi/middleware-audit`) was previously broken under TS 6 strict `isolatedDeclarations` because `Buffer` is a Node-only global. The L2 doc has the full rationale.

> **`ToolRequest.callId` + `ApprovalRequest.callId` (#1759):** Two new optional fields on the L0 middleware contract carrying a per-invocation UI/observability identifier — intentionally separate from `request.metadata`. No `@koi/runtime` wiring change (still integrates `@koi/middleware-permissions` and `@koi/checkpoint` via the same slots), but both L2 packages now read the dedicated `callId` field. `@koi/checkpoint` keeps a `metadata.callId` fallback for legacy callers. The separation keeps approval-cache + in-flight-dedup identity symmetric with backend policy queries; see `docs/L2/middleware-permissions.md` for the rationale and `docs/L2/checkpoint.md` for the `FileOpRecord.callId` correlation details.

> **Stable session-id rotation + checkpoint reset on /clear (#1749):** `@koi/checkpoint` gained a `Checkpoint.resetSession(sessionId)` method that prunes the on-disk chain via the same per-session serializer as `rewind`, mutually exclusive with queued rewind work. `@koi/snapshot-store-sqlite`'s `prune` learned to handle `retainBranches: false` cleanly: head-row updates run BEFORE node deletes (FK ordering), the replacement head is selected from in-memory survivor rows (not a SELECT against still-pre-delete table state), and the in-memory `chainHeads` Map mutation is deferred until AFTER the SQL transaction commits so a rolled-back prune cannot poison the cache. No `@koi/runtime` wiring change — both packages stay in their existing checkpoint stack slot. The new method is consumed by `@koi-agent/cli`'s checkpoint preset stack `onResetSession` hook (gated on `resetContext.truncate === true`); see `docs/L2/checkpoint.md` and `docs/L2/snapshot-store-sqlite.md` for the full contract.


> **`@koi/cost-aggregator` wired as runtime dependency (#1636):** Added as a dependency for the orphan-check gate. The cost aggregator itself runs in the CLI host layer (not middleware), but the runtime declares the dependency so golden-query infrastructure can cover it. Exempt from golden queries (infrastructure, no tool surface).

> **OAuthChannel unification (issue #1982):** `@koi/mcp`'s `McpConnection` interface gained `reconnect()` and optional `triggerAuth?()`. `ConnectionDeps.onUnauthorized` now returns `UnauthorizedOutcome` tri-state (`"refreshed" | "needs-auth" | "transient-failure"`) instead of `void`; new `onAuthNeeded` and `onAuthComplete` deps wire interactive OAuth and post-auth signals. `@koi/fs-nexus`'s `createAuthNotificationHandler` signature changed to `(oauthChannel: OAuthChannel, channel: ChannelAdapter)` — it now routes `auth_required`/`auth_complete` to the `OAuthChannel` and sends `auth_progress` keepalives to the text channel. Hosts that wire `createAuthNotificationHandler` must pass an `OAuthChannel` as the first argument.

## Changelog

- 2026-05-10: `@koi/proactive` adds optional `inbox: InboxSink` to `ProactiveDeliveryConfig` (issue #1301 Phase 4 sub-3). When set, `low`-priority sends call `inbox.enqueue` with an `InboxEnvelope` (`content`, `threadId`, `metadata`, `enqueuedAt`) and return `{ ok: true, delivered: ["inbox"] }`; sync throws and async rejections wrap into `failures: [{ channel: "inbox", error }]`. Inbox writes bypass the empty-channels guard and do not consume the rate-limit window. With no inbox configured, `low` retains Phase-3 single-channel routing. `InboxSink` lives in `@koi/proactive` (L2) — promoted to a shared L0 contract only when a second consumer appears.
- 2026-05-10: `@koi/proactive` adds sequential multi-channel fallback for `high` priority in `createProactiveDelivery` (issue #1301 Phase 4 sub-2). `high` sends now attempt `preferredChannel` first; on adapter failure they walk remaining channels in Map insertion order and return on first success. Rate limit counts each `send()` call as exactly one slot regardless of how many attempts the fallback performs; slot is refunded if every attempt fails. `urgent` keeps parallel fan-out; `normal` and `low` retain single-attempt routing.
- 2026-05-10: `@koi/proactive` adds quiet-hours gating to `createProactiveDelivery` (issue #1301 Phase 4 sub-1). `DeliveryPreferences` accepts `quietHoursStart`, `quietHoursEnd`, `timezone` (IANA, default `"UTC"`); `normal`-priority sends inside the window return `{ ok: false, reason: "quiet_hours" }` without consuming rate-limit capacity. `high` and `urgent` always pass. Cross-midnight windows supported. Validation throws at construction for partial config, out-of-range hours, or invalid timezones. Multi-channel fallback for `high` and inbox routing for `low` remain in follow-up PRs.
- **Path-aware filesystem permissions** — fs_read for out-of-workspace paths triggers permission prompt instead of silent NOT_FOUND.

## #1650 — Soft permission deny (opt-in)

`@koi/permissions` and `@koi/middleware-permissions` now support soft permission deny:

- New L0 field `PermissionDecision.disposition?: "hard" | "soft"` (fail-safe `"hard"` default; see `docs/L2/permissions.md`).
- New rule field `PermissionRule.on_deny?: "hard" | "soft"` (opt-in per rule).
- When the middleware sees `disposition: "soft"` on a deny, it returns a synthetic `ToolResponse` (with `metadata.blockedByHook: true`, `metadata.permissionDenied: true`, `metadata.hookName: "permissions"`) instead of throwing — letting the agent loop continue and adapt. See `docs/L2/middleware-permissions.md` for per-turn retry cap, filter-time visibility rules, `SoftDenyLog`/`DenialRecord` additions, and the golden trajectory (`fixtures/permission-soft-deny.trajectory.json`).

Runtime behavior is unchanged for any rule that does not set `on_deny: "soft"`. No downstream wiring changes are required in this package; observability middleware (`event-trace`, `middleware-report`, `session-transcript`) already treats `blockedByHook` responses as non-executions.

### Phase-2 DX pass (task-tools)

`@koi/task-tools` descriptors were updated with "use PROACTIVELY"
guidance so the agent auto-plans multi-step work. The L3 wiring
itself is unchanged — task tools continue to be contributed via
`executionStack` when the spawn preset is active. See
`docs/L2/task-tools.md` for the descriptor changes.

## #1881 — Bash prefix enrichment (opt-in)

`@koi/middleware-permissions` now supports **prefix-aware permission evaluation** for bash-like tools via the `resolveBashCommand` config option, backed by the new `@koi/bash-classifier` L0u package:

- **Prefix extraction** — ARITY table + wrapper peeling (env, command, exec, nohup, time, timeout, stdbuf, nice, ionice) + shell-aware tokenization derive a stable `<toolId>:<prefix>` key from the raw command. Operators write rules like `allow: Bash:git *` or `ask: Bash:npm run`.
- **`!complex` structural ratchet** — compound commands (redirects, pipelines, subshells, command substitution, env -S) route to a `Bash:!complex` bucket that refuses to silently match broad wildcards, forcing fresh review.
- **Dangerous-command ratchet** — curated pattern registry (privilege-escalation, code-exec, network-exfil, process-spawn, file-destructive, module-load) overrides allow decisions with ask, with an explicit-allow opt-out.
- **Dual-key evaluation** — queries BOTH the enriched `Bash:<prefix>` and plain `Bash` resources; `strictestDecision` merges with default-deny-aware semantics so legacy plain-tool rules keep working.

**Wiring status:** activated in `runtime-factory.ts`. Behavior depends on the backend:

- **TUI (`koi tui`)** — default backend is `createPermissionBackend` (mode-based, not marker-aware). Runs single-key fallback via `allowLegacyBackendBashFallback: true`. Enrichment is wired but prefix rules are NOT enforced — existing TUI policy decisions are unchanged. Ratchets do not fire because the default backend returns `ask` for every bash call (no matching rule), which already requires approval.
- **`koi start`** — uses `createPatternPermissionBackend` with `allow: ["*"]` (marker-aware). Dual-key evaluation engages automatically. Dangerous-command ratchet overrides allow decisions with ask on sudo, `curl | sh`, `python -c`, `node -e`, etc. `!complex` structural ratchet fires on compound forms (redirects, pipelines, subshells, command substitution) instead of silent auto-approve.

Config options exposed at the middleware layer:
- `allowLegacyBackendBashFallback: true` — single-key fallback for legacy backends (prefix rules not enforced).
- `legacyBashGrantFallback: true` — preserve existing durable `bash` approvals during a migration window.

**Config validation:** `validatePermissionsConfig` now rejects `resolveBashCommand` with a non-marker-aware backend unless `allowLegacyBackendBashFallback: true` is set, catching misconfigurations during validation instead of at runtime.

See `docs/L2/bash-classifier.md` and `docs/L2/middleware-permissions.md` for the full design, threat model, and wire protocol.

## #1638 — Inactivity-based stream timeouts

`createRuntime({ activityTimeout })` replaces hard wall-clock kills with activity-based termination. Any adapter event (model chunk, tool call, tool result, turn boundary) resets the idle clock.

```ts
createRuntime({
  adapter,
  activityTimeout: {
    idleWarnMs: 60_000,       // warn at 60s idle (interactive default)
    idleTerminateMs: 120_000, // abort at 2× (default when omitted)
    maxDurationMs: 14_400_000,// 4h wall-clock safety net
    onIdleWarn: (info) => { /* inject system-reminder, telemetry, etc. */ },
    onTerminated: (reason, elapsedMs) => { /* log, page, etc. */ },
  },
});
```

**Telemetry events** are injected into the stream as `EngineEvent` `custom` kinds (exported constants in `@koi/runtime`):

| Type | When | Payload |
|------|------|---------|
| `activity.idle.warning` | idle past `idleWarnMs` | `{ elapsedMs, warnMs, terminateMs }` |
| `activity.terminated.idle` | idle past `idleTerminateMs` → stream aborts | `{ elapsedMs }` |
| `activity.terminated.wall_clock` | `maxDurationMs` exceeded regardless of activity → stream aborts | `{ elapsedMs }` |

**Terminal `done` contract.** On timeout the wrapper always yields a terminal `EngineEvent` of kind `done` with `output.stopReason: "interrupted"` and `output.metadata` carrying `{ terminatedBy: "activity-timeout", terminationReason: "idle" | "wall_clock", elapsedMs }`. Downstream consumers (harness, loop, telemetry) that key off `done.stopReason` see a clean terminal event and can distinguish a timeout-driven interrupt from a user cancel. The wrapper also calls `AbortController.abort("timeout")` so the inner adapter's `signal.reason` is set to the typed `AbortReason`.

**Defaults and back-compat**

- When `activityTimeout` is omitted, the legacy `streamTimeoutMs` is mapped to `maxDurationMs` (default 120s wall-clock) — existing behaviour preserved byte-for-byte.
- When `activityTimeout` is provided but `maxDurationMs` is not, the runtime fills in the legacy 120s (`DEFAULT_STREAM_TIMEOUT_MS`), **not** the recommended 4h. This makes the migration from `streamTimeoutMs` → `activityTimeout` rollback-safe: the hard-stop budget does not silently widen. Callers that want the longer recommended cap must opt in explicitly via `maxDurationMs: DEFAULT_ACTIVITY_MAX_DURATION_MS` (4h constant exported from `@koi/runtime`) or their own value. `maxDurationMs: Number.POSITIVE_INFINITY` disables the wall-clock bound.
- When both `streamTimeoutMs` and `activityTimeout` are provided, `activityTimeout` wins outright.
- `streamTimeoutMs` is now `@deprecated` in favour of `activityTimeout.maxDurationMs`.

**Partial metrics on timeout.** Every accounting field in the synthesized `done.output.metrics` is zeroed (`totalTokens`, `inputTokens`, `outputTokens`, `turns`) so existing aggregators (`@koi/engine`'s `delivery-policy.ts` `RunReport` persistence, the TUI cumulative metrics reducer) cannot be polluted by placeholder numbers from a timed-out run. `durationMs` is authoritative (measured). The rich observability lives in `done.output.metadata`: `terminatedBy: "activity-timeout"`, `terminationReason: "idle" | "wall_clock"`, `elapsedMs`, `metricsSynthesized: true`, and `lastSeenTurnIndex` (highest observed `turn_start` index, or `-1` if none).

**Mid-turn termination envelope.** When the wrapper fires mid-turn, it emits — in order — the `custom: activity.terminated.*` telemetry event, a synthesized `tool_result` for every in-flight tool (output shape: `{ code: "TOOL_EXECUTION_ERROR", error: string, synthesizedBy: "activity-timeout", terminationReason }` — conforms to the existing headless tool-error contract so consumers classify it as a failure), a synthesized `turn_end` with `stopBlocked: true` (reusing the stop-gate marker so `onAfterTurn` middleware does not persist partial state as a real completion), and finally the terminal synthesized `done`. Tools that legitimately keep running past `turn_end` are protected: `pendingTools` stays set until the matching `tool_result` arrives.

**Recommended profiles**

| Profile | `idleWarnMs` | `idleTerminateMs` | `maxDurationMs` |
|---------|--------------|-------------------|------------------|
| Interactive | 60_000 (1 min) | 120_000 (2 min) | 14_400_000 (4h) |
| Batch | 300_000 (5 min) | 600_000 (10 min) | 14_400_000 (4h) |

**Cooperative cancellation hint.** `onIdleWarn` is a synchronous observer callback; hosts wiring it through a middleware can inject a `<system-reminder>` on the next prompt ("you've been idle for N seconds, are you stuck?"). The core runtime does not ship such a middleware — it is a per-deployment choice.

<!-- #1769: watch_patterns E2E touches this package (createdBy/lastAssignedTo, task_output ACL + matches_only, sandbox-os callback cap-survival, turn-prelude middleware wiring). -->

> **Progressive skill disclosure (PR #1986, issue #1986):** `@koi/skills-runtime`'s `createSkillProvider()` and `createSkillInjectorMiddleware()` now accept `{ progressive: true }`. In progressive mode the provider attaches skills with `runtimeBacked: true` and `content: ""` (no body loaded into the system prompt at startup). The injector middleware instead injects a compact `<available_skills>` XML block listing skill names and descriptions (~100 tokens) before each model call; the full body is loaded on-demand when the model invokes the `Skill` tool. The `skill-load` golden cassette re-recorded: `MW:skill-injector` span now shows `contentLength: 0` and a `systemPrompt` that opens with `<available_skills>` XML rather than the full body. New `loop-until-pass` cassette added (feedback-loop retry pattern). No `RuntimeConfig` surface change — hosts opt in by passing `{ progressive: true }` to both factories.

---

## @koi/tasks RemoteAgentTask lifecycle hardening (#2043)

`@koi/tasks` received correctness fixes for the `remote_agent` lifecycle that affect
observable runtime behaviour. No new golden queries or cassettes were required (no new
tools or middleware); the 2 new `@koi/tasks` golden-replay tests are standalone and
require no LLM.

Changes visible at the `@koi/runtime` integration boundary:

- **Composite stop key**: `TaskRunner` now scopes stop-suppression to
  `${taskId}:${attemptId}`. A retry attempt on the same `taskId` will no longer
  inherit stop-suppression from a prior attempt, so natural exits from retry B
  correctly transition the board to `completed`/`failed`.
- **Cancel-notify observability**: explicit `stop()` now awaits cancel-notify
  delivery before the board `kill` transition. Failure notes (`[cancel-notify:
  failed — HTTP NNN]`) land in the task's output stream and are readable via
  `readOutput()` after the stop call returns.

---

## @koi/temporal `WorkerConfig` API (PR #2052)

`createTemporalWorker` now accepts a flat `WorkerConfig` object `{ taskQueue, url?, namespace?, maxCachedWorkflows? }` as its first argument (replacing the previous `{ config: TemporalConfig }` wrapper). The argument order is `(config, activities, workflowsPath, factory?)`. `WorkerConfig` is exported from `@koi/temporal` for downstream typing.

`createTemporalScheduler` now implements `TaskScheduler.querySchedules(agentId)` returning all cron schedules for a given agent.


## Changelog

- 2026-05-10: `@koi/proactive` adds `createProactiveDelivery` (issue #1301 Phase 3). Priority-routing dispatcher over existing `ChannelAdapter`s — `urgent` fans out to all channels and bypasses rate limits; `high`/`normal`/`low` route to `preferredChannel` (or first by Map insertion order) and are gated by a sliding 1-hour `maxNotificationsPerHour` cap. Adapter exceptions are wrapped into `failures[]`; failed deliveries refund their rate slot. Quiet hours, multi-channel fallback, and inbox-routing for `low` are deferred to Phase 4. No `RuntimeConfig` surface change — hosts wire `createProactiveDelivery` themselves and pass the channel map.
- 2026-05-10: `@koi/proactive` governance trigger ID and novelty pattern key both include `limit` so distinct thresholds on the same sensor (warning + critical bands, paired above/below windows) do not deduplicate against each other. `mapGovernanceSignal` now produces IDs of the form `governance:<sensor>:<direction>:<limit>:<emittedAt>`; `defaultPatternKey` for `threshold_crossed` now ends with `|<direction>|<limit>` instead of `|<direction>`. Without this, the executor's idempotency layer treated a critical alert as a replay of an earlier warning, and repeated success on a low-severity warning could auto-approve a materially different critical-threshold plan via shared novelty credit. No `RuntimeConfig` surface change.
- 2026-05-10: Golden coverage wired for `@koi/proactive` sleep/cron tools (issue #1298 follow-up). New `proactive-tools` ATIF trajectory exercises `sleep` + `schedule_cron` against the shared in-memory SchedulerComponent; four standalone `Golden: @koi/proactive` tests validate the tool surface, `sleep` → `scheduler.query` pending state, `schedule_cron` + `cancel_schedule` round-trip, and the trajectory's tool_call expectations. Also adds three `SystemSignalSource` adapters in `@koi/proactive` (`createGovernanceSignalSource`, `createGroveSignalSource`, `createNexusSignalSource`) with a defensive close-aware shared async emitter that catches handler exceptions and routes to `onError`. No `RuntimeConfig` surface change — adapters are opt-in observability primitives consumed by `CompositionPlanner`.
- 2026-05-09: `@koi/proactive` integrated as L2 dep (issues #1300, #1195). Adds the `CompositionExecutor` (idempotent multi-step plan execution against scheduler/notify/spawn/forge/toolCall handlers) and the 5-component governance gate (confidence threshold, cost cap, delegation check, per-session rate limit, novel-pattern guard) that runs before any plan executes. Three golden replay tests cover low-confidence denial, `recordGap` on missing `tool_call` handler, and session rate-limit blocking after the configured cap. No `RuntimeConfig` surface change — proactive is opt-in: hosts wire `createCompositionExecutor` themselves and supply trackers.
- 2026-05-08: Dynamic Nexus mounts wired into the runtime (issue #1987). New middleware `createMountDescriptionsMiddleware` injects a stable `<mounted_connectors>` block plus an append-only `<runtime_mounted_connectors>` block into the system prompt, ordered after `<available_skills>` to keep the prompt cache warm. `resolveFileSystemAsync` now wraps the `@koi/fs-nexus` local transport in `guardedTransport` (read-only / scope / protected-root / overlap / quarantine) before exposing `addMount` / `removeMount` / `listMounts` / `describeMount`; the local transport defaults to omitting those RPCs unless `enableMountMutations: true` is set, so only the resolver-mediated path can mutate topology. `MountDescriptionsState` honors `strictPromptIdentifiers`: backend-controlled identifiers failing the `[A-Za-z0-9._-]` allowlist (with `.` / `..` segment rejection) are omitted from the rendered prompt block while remaining in the operator-facing snapshot.
- 2026-05-08: `@koi/temporal` durable workflows + cron-dispatch routing (issue #1356, PR #2165). Adds `agentWorkflow`, `scheduledTaskWorkflow`, `retryWorkflow` definitions and a worker-factory entry point. Cron-fired dispatch wrappers now signal the long-running per-agent workflow at `workflowId=agentId` (matching the `signalWithStart` semantic used by direct `submit-dispatch`); cron-fired spawn wrappers exit fast under `ParentClosePolicy.ABANDON` so SKIP overlap never drops ticks. Agent workflow accepts a `messages` signal whose handler appends to the in-flight queue; the loop's runaway guard is bound on observed work (not iteration count). `runScheduledTask` retries are owned by a single child workflow with caller-provided `maxAttempts`/`backoffMs` (fixed-interval). New env-gated e2e suite (`TEMPORAL_E2E_ADDRESS`) exercises signal-into-running-workflow and ABANDON-child paths against a real Temporal server. No `RuntimeConfig` surface change — workflows are consumed by hosts that wire a Temporal worker.
- 2026-05-08: `@koi/scratchpad-nexus` and `@koi/workspace-nexus` integrated as L2 deps (issue #1373). Both implement the same L0 contracts as their local peers (`@koi/scratchpad-local`, `@koi/workspace`) via Nexus JSON-RPC transport. Two new test-only L2 packages ship alongside: `@koi/scratchpad-conformance` and `@koi/workspace-conformance` extract the shared contract suite so any future backend implementation passes the same tests. Adapters are `koi: { optional: true }` — wired but not loaded by default; operators opt in by passing the transport. No new tools, middleware, or model-loop behavior.
- 2026-05-08: `@koi/middleware-otel` exports retry metadata on spans (refs #1413). `buildModelSpanAttrs`/`buildToolSpanAttrs` propagate `retryOfTurn`/`retryAttempt`/`retryFailureClass`/`retryReason` from step metadata to OTel attributes (`koi.retry.*`). No `RuntimeConfig` change — purely additive on existing `RuntimeConfig.otel` wiring.
- 2026-05-07: `@koi/sandbox-cloudflare` and `@koi/sandbox-wasm` integrated as L2 deps (#1377). Both implement the new `EdgeFunctionAdapter` contract from `@koi/core`. `@koi/sandbox-cloudflare`'s `create()` is currently a hard stub (`UNAVAILABLE`/`ADAPTER_NOT_IMPLEMENTED`) gated behind `EXPERIMENTAL_createCloudflareAdapter`; the two-worker shim templates, `KoiDedupeDO` state machine, runtime fence, and dedupe-fingerprint helpers ship in this commit, with the wrangler/Workers-API deploy path landing in a follow-up. `@koi/sandbox-wasm` ships a working in-process executor for trusted class-A workloads (advisory `timeoutMs`, host-owned linear memory enforced via static section scan, FIFO module cache). `@koi/sandbox-vercel` is **NOT wired** — it ships `designOnly: true` so its KV-state-machine + per-pair Ed25519 helpers + shim templates are reviewable without the deploy path.
- 2026-05-03: `@koi/dashboard-client` integrated as L2 dep (#1381) and `@koi/dashboard-types` as L0u dep. SDK is read-only HTTP/WS over the dashboard API and sits outside the agent loop, so it is `GOLDEN_QUERY_EXEMPT` (mirrors the gateway-* SDKs). No new tools, middleware, hooks, or model-loop behavior — re-export only.
- 2026-04-30: `@koi/eval` integrated as L2 dep (#1380). `tsconfig.json` adds `DOM` to `lib` so types pulled in via `@koi/governance-scope` (`URLPattern`) resolve under `--noEmit`.
- 2026-04-29: Adversarial review hardening (#1378) — bounded shutdown waits, pre-await accounting, identity-based dedup (transport+name), allowlist-based env exposure, reused in-flight scans, and lint cleanup.
- 2026-05-11: `@koi/approval-zones` integrated as L2 dep (#1644). New L2 surface for risk-tier-gated zone interception in the permissions ask path; covered by two standalone golden queries in `golden-replay.test.ts`. No new tools, middleware, hooks, or model-loop behavior — runtime exposes the package via the existing graph; consumers opt in by passing `zones: { evaluator }` to `createPermissionsMiddleware`.
